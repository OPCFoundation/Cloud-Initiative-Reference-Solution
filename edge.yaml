apiVersion: v1
kind: Namespace
metadata:
  name: edge
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ua-edgetranslator
  namespace: edge
spec:
  replicas: 1
  # Single instance with hostPath volumes (settings, nodesets, pki, logs). The
  # default RollingUpdate strategy starts the replacement pod BEFORE terminating
  # the old one, so two servers would briefly share the same PKI/settings
  # directories and both try to bind the OPC UA port. Recreate terminates first.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ua-edgetranslator
  template:
    metadata:
      labels:
        app: ua-edgetranslator
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      initContainers:
      # The UA Edge Translator image runs as the non-root "app" user (USER app in
      # its Dockerfile), but the /translator/* hostPath volumes are created by
      # kubelet as root:root 0755. Without this the server cannot write its OPC UA
      # application certificate into /app/pki, so the OPC UA listener never starts
      # on 4840 - while the ASP.NET process stays alive and the pod misleadingly
      # reports 1/1 Running. Chown by NAME so we do not depend on the numeric UID.
      - name: init-chown-data
        image: ghcr.io/opcfoundation/ua-edgetranslator:main
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 0
        command: ["/bin/sh", "-c"]
        args:
          - |
            set -e
            chown -R app:app /app/settings /app/nodesets /app/pki /app/logs
            echo "Ownership of the translator hostPath volumes fixed"
        volumeMounts:
        - name: "settings"
          mountPath: "/app/settings"
        - name: "nodesets"
          mountPath: "/app/nodesets"
        - name: "pki"
          mountPath: "/app/pki"
        - name: "logs"
          mountPath: "/app/logs"
      # Wait for the simulated Modbus device so the Thing Description can be
      # onboarded successfully on the very first startup pass. The wait is BOUNDED:
      # the simulator is only a demo asset, so it must never permanently block the
      # Edge Translator (and, transitively, the Cloud Publisher). If it is not up in
      # time we start anyway - the translator retries asset connections by itself.
      - name: wait-for-modbus
        image: busybox:1.38
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-c"]
        args:
          - |
            echo "Waiting for modbus-simulator.munich:502 (max 300s) ..."
            timeout 300 sh -c 'until nc -z -w 2 modbus-simulator.munich 502; do sleep 5; done' \
              && echo "Modbus simulator is up" \
              || echo "WARNING: modbus-simulator.munich:502 not reachable - starting anyway"
            exit 0
      # Seed the WoT Thing Description into the settings folder. UA Edge Translator
      # onboards every *.jsonld found there at startup. Copied only when absent so
      # assets added or edited through the UI are preserved across restarts.
      - name: seed-thing-descriptions
        image: busybox:1.38
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-c"]
        args:
          - |
            if [ ! -f /app/settings/modbus-simulator.jsonld ]; then
              echo "Seeding WoT Thing Description for the Modbus simulator"
              cp /seed/modbus-simulator.jsonld /app/settings/modbus-simulator.jsonld
            else
              echo "modbus-simulator.jsonld already present; leaving existing asset untouched"
            fi
            # This container runs as root, so anything it just copied is root-owned.
            # The translator runs as the non-root "app" user (UID/GID 1000 in the
            # .NET base image) and must be able to rewrite its asset files.
            chown -R 1000:1000 /app/settings
        volumeMounts:
        - name: "settings"
          mountPath: "/app/settings"
        - name: "thing-descriptions"
          mountPath: "/seed"
      # UA Edge Translator loads its southbound protocol drivers (Modbus, BACnet,
      # OPC UA, MQTT, S7, EtherNet/IP, ...) as plain .NET assemblies from
      # /app/drivers. They ship in a separate image so the driver pack can be
      # revved independently of the server. Without this the translator starts
      # with no protocol support and cannot onboard any asset.
      - name: ua-edgetranslator-drivers
        image: ghcr.io/opcfoundation/ua-edgetranslator-drivers:main
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-c", "cp -a /drivers/. /app/drivers/"]
        volumeMounts:
        - name: "drivers"
          mountPath: "/app/drivers"
      containers:
      - name: ua-edgetranslator
        image: ghcr.io/opcfoundation/ua-edgetranslator:main
        imagePullPolicy: IfNotPresent
        env:
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        - containerPort: 4840
          name: opcua
        - containerPort: 5000
        - containerPort: 5001
        - containerPort: 19520
        - containerPort: 19521
        # Blazor diagnostics / configuration UI (DiagnosticsWebHost binds 8081).
        - containerPort: 8081
          name: http
        # The image ships a Docker HEALTHCHECK against 4840, but Kubernetes ignores
        # that - without an explicit probe the pod reports Ready while the OPC UA
        # listener may not be up at all.
        readinessProbe:
          tcpSocket:
            port: opcua
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 30
        livenessProbe:
          tcpSocket:
            port: opcua
          initialDelaySeconds: 120
          periodSeconds: 30
          failureThreshold: 5
        volumeMounts:
        - name: "settings"
          mountPath: "/app/settings"
        - name: "nodesets"
          mountPath: "/app/nodesets"
        - name: "pki"
          mountPath: "/app/pki"
        - name: "logs"
          mountPath: "/app/logs"
        - name: "drivers"
          mountPath: "/app/drivers"
      volumes:
        - name: "settings"
          hostPath:
            path: "/translator/settings"
        - name: "thing-descriptions"
          configMap:
            name: modbus-thing-description
            items:
              - key: modbus-simulator.jsonld
                path: modbus-simulator.jsonld
        - name: "nodesets"
          hostPath:
            path: "/translator/nodesets"
        - name: "pki"
          hostPath:
            path: "/translator/pki"
        - name: "logs"
          hostPath:
            path: "/translator/logs"
        # Populated by the ua-edgetranslator-drivers init container on every pod
        # start, so the driver pack always matches the image that was pulled.
        - name: "drivers"
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: ua-edgetranslator
  namespace: edge
spec:
  type: LoadBalancer
  ports:
  - name: opcua
    targetPort: 4840
    port: 4840
  - name: lorawan
    targetPort: 5000
    port: 5000
  - name: securelorawan
    targetPort: 5001
    port: 5001
  - name: ocpp
    targetPort: 19520
    port: 19520
  - name: secureocpp
    targetPort: 19521
    port: 19521
  # NOTE: the web UI (8080) is deliberately NOT here. It is served over HTTPS
  # through the ingress at the bottom of this file, because it accepts Basic
  # credentials that must not cross the LAN in cleartext. The protocol ports
  # above stay as they are: they are raw TCP spoken by field devices and by
  # UA Cloud Publisher, not HTTP, so an HTTP ingress cannot carry them.
  selector:
    app: ua-edgetranslator
---
# Internal Service for the Edge Translator web UI. ClusterIP, so the plain-HTTP
# port is never bound on the node; the ingress below is the only way in.
apiVersion: v1
kind: Service
metadata:
  name: ua-edgetranslator-ui
  namespace: edge
spec:
  type: ClusterIP
  ports:
  - name: http
    targetPort: 8081
    port: 8080
  selector:
    app: ua-edgetranslator
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ua-cloudpublisher
  namespace: edge
spec:
  replicas: 1
  # Single instance with hostPath volumes (settings, pki, logs). Two publishers
  # sharing /publisher/settings would race on settings.json and both publish the
  # same DataSetWriters. Recreate terminates the old pod before starting the new.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ua-cloudpublisher
  template:
    metadata:
      labels:
        app: ua-cloudpublisher
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      initContainers:
      # Wait until every simulated production line station is accepting OPC UA
      # connections, so the Publisher's auto-loaded persistency.json can subscribe
      # to them straight away instead of failing on first connect.
      - name: wait-for-productionline
        image: busybox:1.38
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-c"]
        args:
          - |
            # NOTE: deliberately no shell variables here. This manifest is applied
            # through envsubst, which would replace an unprotected "$station" with
            # an empty string and leave the pod waiting on ".munich" forever.
            # The wait is BOUNDED so a missing station cannot block startup forever;
            # the Publisher retries its OPC UA connections by itself.
            echo "Waiting for the munich production line (max 300s) ..."
            timeout 300 sh -c 'until nc -z -w 2 mes.munich 4840 \
                                  && nc -z -w 2 assembly.munich 4840 \
                                  && nc -z -w 2 test.munich 4840 \
                                  && nc -z -w 2 packaging.munich 4840; do sleep 5; done' \
              && echo "Production line 'munich' is ready" \
              || echo "WARNING: not all munich stations reachable - starting anyway"
            exit 0
      # Wait for UA Edge Translator's OPC UA server as well: the persistency file
      # also subscribes to the Modbus asset it maps into OPC UA.
      - name: wait-for-edgetranslator
        image: busybox:1.38
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-c"]
        args:
          - |
            echo "Waiting for ua-edgetranslator.edge:4840 (max 300s) ..."
            timeout 300 sh -c 'until nc -z -w 2 ua-edgetranslator.edge 4840; do sleep 5; done' \
              && echo "UA Edge Translator is up" \
              || echo "WARNING: ua-edgetranslator.edge:4840 not reachable - starting anyway"
            exit 0
      # Seed the broker connection / metadata configuration and the published-nodes
      # persistency on first start so the "Configuring the Broker Connection and
      # Metadata" UI steps are not needed and data flows immediately.
      # Files are only copied if the operator has not already saved settings,
      # so any later changes made through the UI are preserved.
      - name: seed-settings
        image: busybox:1.38
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-c"]
        args:
          - |
            if [ ! -f /app/settings/settings.json ]; then
              echo "Seeding default UA Cloud Publisher settings.json"
              cp /seed/settings.json /app/settings/settings.json
            else
              echo "settings.json already present; leaving existing configuration untouched"
            fi
            if [ ! -f /app/settings/persistency.json ]; then
              echo "Seeding default UA Cloud Publisher persistency.json (Munich production line)"
              cp /seed/persistency.json /app/settings/persistency.json
            else
              echo "persistency.json already present; leaving existing published nodes untouched"
            fi
        volumeMounts:
        - name: "settings"
          mountPath: "/app/settings"
        - name: "settings-seed"
          mountPath: "/seed"
      containers:
      - name: ua-cloudpublisher
        image: ghcr.io/barnstee/ua-cloudpublisher:main
        imagePullPolicy: IfNotPresent
        env:
        - name: PUBLISHER_USERNAME
          value: "${IOT_USERNAME}"
        - name: PUBLISHER_PASSWORD
          value: "${IOT_PASSWORD}"
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: "settings"
          mountPath: "/app/settings"
        - name: "pki"
          mountPath: "/app/pki"
        - name: "logs"
          mountPath: "/app/logs"
        - name: "store"
          mountPath: "/app/store"
      volumes:
        - name: "settings"
          hostPath:
            path: "/publisher/settings"
        - name: "settings-seed"
          configMap:
            name: ua-cloudpublisher-settings
            items:
              - key: settings.json
                path: settings.json
              - key: persistency.json
                path: persistency.json
        - name: "pki"
          hostPath:
            path: "/publisher/pki"
        - name: "logs"
          hostPath:
            path: "/publisher/logs"
        - name: "store"
          hostPath:
            path: "/publisher/store"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ua-cloudpublisher-settings
  namespace: edge
data:
  # Pre-populated UA Cloud Publisher configuration that points the publisher at
  # the in-cluster Mosquitto broker (TLS, 8883) and enables OPC UA metadata
  # messages. Credentials are substituted from ${IOT_USERNAME}/${IOT_PASSWORD}
  # at apply time (envsubst), matching the rest of the stack.
  # AllowUntrustedBrokerCertificate is required because Mosquitto uses a
  # self-signed demo CA generated at deploy time (see gen-certs in cloud.yaml);
  # this mirrors Telegraf's insecure_skip_verify and UA Cloud Action's
  # MQTT_TLS_INSECURE. Replace the broker cert with one from a trusted CA and
  # set this back to false for production.
  settings.json: |
    {
      "PublisherName": "UACloudPublisher",
      "BrokerUrl": "mosquitto.cloud.svc.cluster.local",
      "BrokerPort": 8883,
      "BrokerUsername": "${IOT_USERNAME}",
      "BrokerPassword": "${IOT_PASSWORD}",
      "BrokerMessageTopic": "data",
      "BrokerMetadataTopic": "metadata",
      "SendUAMetadata": true,
      "UseTLS": true,
      "AllowUntrustedBrokerCertificate": true,
      "UseKafka": false,
      "CreateBrokerSASToken": false,
      "UseUACertAuth": false,
      "UseCustomCertAuth": false,
      "UseAltBrokerForMetadata": false,
      "AltBrokerUrl": null,
      "AltBrokerPort": 8883,
      "AltBrokerUsername": null,
      "AltBrokerPassword": null,
      "SendUAStatus": false,
      "SendUAConnection": false,
      "SendDiagnosticsMessages": true,
      "MetadataSendInterval": 300,
      "BrokerCommandTopic": null,
      "BrokerDataReceivedTopic": null,
      "BrokerResponseTopic": null,
      "BrokerMessageSize": 262144,
      "UseReverseConnect": false,
      "AutoPublishAllWoTProperties": true,
      "PushCertsBeforePublishing": true,
      "InternalQueueCapacity": 1000,
      "DefaultSendIntervalSeconds": 1,
      "DiagnosticsLoggingInterval": 30,
      "DefaultOpcSamplingInterval": 500,
      "DefaultOpcPublishingInterval": 1000,
      "UAStackTraceMask": 645,
      "ReversiblePubSubEncoding": false,
      "OmitNetworkMessageHeader": false,
      "UseCloudEventsMetadataHeader": false,
      "AutoLoadPersistedNodes": true,
      "AzureOpenAIAPIEndpoint": null,
      "AzureOpenAIAPIKey": null,
      "AzureOpenAIDeploymentName": null
    }
  # Published-nodes persistency seeded so the Publisher immediately subscribes to
  # the simulated Munich production line stations (auto-loaded because
  # AutoLoadPersistedNodes is true above).
  #
  # The production line stations accept anonymous sessions for reading, but the
  # UA Edge Translator requires a user token for its node subscriptions, so that
  # endpoint uses UsernamePassword with the shared IOT credentials. Note the
  # Publisher stores this password encrypted with its issuer certificate when it is
  # written from the UI, but on load DecryptString() falls back to using the value
  # verbatim when it is not valid Base64 - which is what makes a seeded plaintext
  # password work here.
  #
  # UA Cloud Commander is listed as well. It is itself an OPC UA server
  # (Opc.Ua.Cloud.Commander.UAServer derives from StandardServer) but exposes no
  # custom address space, so the only node subscribed is the standard
  # Server_ServerStatus_CurrentTime (i=2258) - a liveness heartbeat. The real
  # purpose of the entry is to make the Publisher include it in the GDS Server
  # Push loop, which only iterates the endpoints found in this file, so the
  # Commander gets a CA-issued certificate and issuer list like every other
  # server in the stack.
  #
  # NOTE: this deliberately subscribes to CurrentTime (i=2258) and NOT to
  # Server_ServerStatus_State (i=2259). State never changes - it reads "Running"
  # forever - so that subscription had no data updates at all and depended purely
  # on server keep-alives. The parked Publish request then expired before the
  # keep-alive was due, and BOTH ends logged a fault every 100 seconds, forever:
  #   publisher: Publish #NNNN ... Error: ... (BadTimeout)
  #   commander: SERVER - Service Fault Occurred. Reason=BadTimeout
  # CurrentTime ticks continuously, so real data always arrives well inside the
  # timeout - and a moving timestamp is a far better liveness signal than a
  # constant. Nothing was ever lost, but the logs were unusable.
  persistency.json: |
    [
      {
        "EndpointUrl": "opc.tcp://mes.munich/",
        "OpcNodes": [
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station-MES/;s=Morning",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station-MES/;s=Afternoon",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station-MES/;s=Night",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          }
        ],
        "OpcEvents": [],
        "OpcAuthenticationMode": "Anonymous"
      },
      {
        "EndpointUrl": "opc.tcp://assembly.munich/",
        "OpcNodes": [
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=379",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=385",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=391",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=398",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=399",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=400",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=406",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=412",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=418",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=434",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          }
        ],
        "OpcEvents": [],
        "OpcAuthenticationMode": "Anonymous"
      },
      {
        "EndpointUrl": "opc.tcp://test.munich/",
        "OpcNodes": [
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=379",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=385",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=391",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=398",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=399",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=400",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=406",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=412",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=418",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=434",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          }
        ],
        "OpcEvents": [],
        "OpcAuthenticationMode": "Anonymous"
      },
      {
        "EndpointUrl": "opc.tcp://packaging.munich/",
        "OpcNodes": [
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=379",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=385",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=391",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=398",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=399",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=400",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=406",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=412",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=418",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/Station/;i=434",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          }
        ],
        "OpcEvents": [],
        "OpcAuthenticationMode": "Anonymous"
      },
      {
        "EndpointUrl": "opc.tcp://ua-edgetranslator.edge.svc.cluster.local:4840",
        "OpcNodes": [
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=Temperature",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=Pressure",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=FlowRate",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=EnergyConsumption",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=MotorSpeed",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=MachineState",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=Running",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          },
          {
            "Id": "nsu=http://opcfoundation.org/UA/modbus-simulator/;s=FaultActive",
            "OpcSamplingInterval": 500,
            "OpcPublishingInterval": 1000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          }
        ],
        "OpcEvents": [],
        "OpcAuthenticationMode": "UsernamePassword",
        "UserName": "${IOT_USERNAME}",
        "Password": "${IOT_PASSWORD}"
      },
      {
        "EndpointUrl": "opc.tcp://ua-cloudcommander.edge.svc.cluster.local:4840",
        "OpcNodes": [
          {
            "Id": "i=2258",
            "OpcSamplingInterval": 5000,
            "OpcPublishingInterval": 10000,
            "HeartbeatInterval": 0,
            "SkipFirst": false
          }
        ],
        "OpcEvents": [],
        "OpcAuthenticationMode": "UsernamePassword",
        "UserName": "${IOT_USERNAME}",
        "Password": "${IOT_PASSWORD}"
      }
    ]
---
apiVersion: v1
kind: Service
metadata:
  name: ua-cloudpublisher
  namespace: edge
spec:
  # ClusterIP: this UI accepts Basic credentials, so it is reachable only over
  # HTTPS through the ingress at the bottom of this file.
  type: ClusterIP
  ports:
  - name: http
    targetPort: 8080
    port: 8081
  selector:
    app: ua-cloudpublisher
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ua-cloudcommander
  namespace: edge
spec:
  replicas: 1
  # Single instance with hostPath volumes; see the note on ua-edgetranslator.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ua-cloudcommander
  template:
    metadata:
      labels:
        app: ua-cloudcommander
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: ua-cloudcommander
        image: ghcr.io/opcfoundation/ua-cloudcommander:main
        imagePullPolicy: IfNotPresent
        env:
        # Broker connection (Mosquitto, TLS MQTT on 8883 with username/password auth)
        - name: BROKERNAME
          value: "mosquitto.cloud.svc.cluster.local"
        - name: BROKERPORT
          value: "8883"
        - name: USE_TLS
          value: "true"
        # Mosquitto uses a self-signed demo CA generated at deploy time (see
        # gen-certs in cloud.yaml), so chain validation cannot succeed. This
        # mirrors Telegraf's insecure_skip_verify and UA Cloud Action's
        # MQTT_TLS_INSECURE. Remove it once the broker presents a cert from a
        # CA the client trusts.
        - name: ALLOW_UNTRUSTED_BROKER_CERT
          value: "true"
        - name: CLIENTNAME
          value: "UACloudCommander"
        # Topic to subscribe to for incoming ua-action-request messages
        - name: TOPIC
          value: "commands"
        # Default topic to publish ua-action-response messages to
        - name: RESPONSE_TOPIC
          value: "responses"
        # Broker credentials (same IOT_USERNAME / IOT_PASSWORD used across the stack)
        - name: USERNAME
          value: "${IOT_USERNAME}"
        - name: PASSWORD
          value: "${IOT_PASSWORD}"
        # Credentials used to connect to the target OPC UA servers (e.g. the Edge Translator)
        - name: UA_USERNAME
          value: "${IOT_USERNAME}"
        - name: UA_PASSWORD
          value: "${IOT_PASSWORD}"
        # Credentials for UA Cloud Commander's OWN OPC UA server. Its
        # VerifyPassword() compares the presented user token against these two
        # variables and maps a match to SystemConfigurationIdentity (SecurityAdmin),
        # which is what the Publisher's GDS Server Push requires. Without them every
        # push is rejected with BadUserAccessDenied.
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        # UA Cloud Commander is itself an OPC UA server (StandardServer), so it can be
        # provisioned by GDS Server Push like any other server in the stack.
        - containerPort: 4840
          name: opcua
        volumeMounts:
        - name: "pki"
          mountPath: "/app/pki"
        - name: "logs"
          mountPath: "/app/logs"
      volumes:
        - name: "pki"
          hostPath:
            path: "/commander/pki"
        - name: "logs"
          hostPath:
            path: "/commander/logs"
---
apiVersion: v1
kind: Service
metadata:
  name: ua-cloudcommander
  namespace: edge
spec:
  ports:
  - name: opcua
    targetPort: 4840
    port: 4840
  selector:
    app: ua-cloudcommander
---
# ---------------------------------------------------------------------------
# Simulated production line "Munich"
#
# A software-only factory simulation from the Digital Twin Consortium
# Manufacturing Ontologies reference solution. It runs four OPC UA servers that
# together model a production line:
#   * mes       - Manufacturing Execution System (shift schedule)
#   * assembly  - assembly station
#   * test      - test station
#   * packaging - packaging station
#
# Each station is an OPC UA server listening on port 4840. They live in their own
# "munich" namespace so their in-cluster DNS names match the OPC UA application
# URIs the stations advertise (opc.tcp://<station>.munich/), which is exactly what
# the seeded UA Cloud Publisher persistency.json subscribes to.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: munich
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mes
  namespace: munich
spec:
  replicas: 1
  # Single instance with a hostPath volume; see the note on ua-edgetranslator.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mes
  template:
    metadata:
      labels:
        app: mes
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: mes
        image: ghcr.io/digitaltwinconsortium/manufacturingontologies:main
        imagePullPolicy: IfNotPresent
        env:
        - name: StationType
          value: "mes"
        - name: StationURI
          value: "opc.tcp://mes.munich/"
        - name: FactoryTimeZone
          value: "Europe/Berlin"
        - name: ProductionLineName
          value: "munich"
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        - containerPort: 4840
          name: opcua
        volumeMounts:
        - name: "pki"
          mountPath: "/app/pki"
      volumes:
        - name: "pki"
          hostPath:
            path: "/productionline/munich/mes/pki"
---
apiVersion: v1
kind: Service
metadata:
  name: mes
  namespace: munich
spec:
  ports:
  - name: opcua
    targetPort: 4840
    port: 4840
  selector:
    app: mes
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assembly
  namespace: munich
spec:
  replicas: 1
  # Single instance with a hostPath volume; see the note on ua-edgetranslator.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: assembly
  template:
    metadata:
      labels:
        app: assembly
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: assembly
        image: ghcr.io/digitaltwinconsortium/manufacturingontologies:main
        imagePullPolicy: IfNotPresent
        env:
        - name: StationType
          value: "assembly"
        - name: StationURI
          value: "opc.tcp://assembly.munich/"
        - name: PowerConsumption
          value: "200"
        - name: CycleTime
          value: "6"
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        - containerPort: 4840
          name: opcua
        volumeMounts:
        - name: "pki"
          mountPath: "/app/pki"
      volumes:
        - name: "pki"
          hostPath:
            path: "/productionline/munich/assembly/pki"
---
apiVersion: v1
kind: Service
metadata:
  name: assembly
  namespace: munich
spec:
  ports:
  - name: opcua
    targetPort: 4840
    port: 4840
  selector:
    app: assembly
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  namespace: munich
spec:
  replicas: 1
  # Single instance with a hostPath volume; see the note on ua-edgetranslator.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: test
        image: ghcr.io/digitaltwinconsortium/manufacturingontologies:main
        imagePullPolicy: IfNotPresent
        env:
        - name: StationType
          value: "test"
        - name: StationURI
          value: "opc.tcp://test.munich/"
        - name: PowerConsumption
          value: "100"
        - name: CycleTime
          value: "6"
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        - containerPort: 4840
          name: opcua
        volumeMounts:
        - name: "pki"
          mountPath: "/app/pki"
      volumes:
        - name: "pki"
          hostPath:
            path: "/productionline/munich/test/pki"
---
apiVersion: v1
kind: Service
metadata:
  name: test
  namespace: munich
spec:
  ports:
  - name: opcua
    targetPort: 4840
    port: 4840
  selector:
    app: test
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: packaging
  namespace: munich
spec:
  replicas: 1
  # Single instance with a hostPath volume; see the note on ua-edgetranslator.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: packaging
  template:
    metadata:
      labels:
        app: packaging
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: packaging
        image: ghcr.io/digitaltwinconsortium/manufacturingontologies:main
        imagePullPolicy: IfNotPresent
        env:
        - name: StationType
          value: "packaging"
        - name: StationURI
          value: "opc.tcp://packaging.munich/"
        - name: PowerConsumption
          value: "100"
        - name: CycleTime
          value: "6"
        - name: OPCUA_USERNAME
          value: "${IOT_USERNAME}"
        - name: OPCUA_PASSWORD
          value: "${IOT_PASSWORD}"
        ports:
        - containerPort: 4840
          name: opcua
        volumeMounts:
        - name: "pki"
          mountPath: "/app/pki"
      volumes:
        - name: "pki"
          hostPath:
            path: "/productionline/munich/packaging/pki"
---
apiVersion: v1
kind: Service
metadata:
  name: packaging
  namespace: munich
spec:
  ports:
  - name: opcua
    targetPort: 4840
    port: 4840
  selector:
    app: packaging
---
# ---------------------------------------------------------------------------
# Simulated Modbus TCP device ("line conditioning unit") on the Munich line
#
# A small, dependency-free Modbus TCP server that exposes a handful of live,
# continuously changing registers. It demonstrates how UA Edge Translator maps a
# NON-OPC UA device into OPC UA using a W3C WoT Thing Description (see the
# 'modbus-thing-description' ConfigMap below).
#
# Implemented with the Python standard library only (no pip install), so the
# stock python:3.12-slim image works on arm64 (Raspberry Pi) and offline.
#
# Register map (unit id 1), all values big-endian as per the Modbus standard:
#   Holding registers (FC3)
#     0-1  Temperature        float32  degC
#     2-3  Pressure           float32  bar
#     4-5  FlowRate           float32  l/min
#     6-7  EnergyConsumption  float32  kWh (monotonically increasing)
#     8    MotorSpeed         int16    rpm
#     9    MachineState       int16    0=stopped 1=running 2=fault
#   Coils (FC1)
#     0    Running            bool
#     1    FaultActive        bool
# ---------------------------------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: modbus-simulator
  namespace: munich
data:
  modbus_server.py: |
    #!/usr/bin/env python3
    """Minimal Modbus TCP simulation server (standard library only).

    Supports the read/write function codes needed by UA Edge Translator:
      1  Read Coils
      2  Read Discrete Inputs
      3  Read Holding Registers
      4  Read Input Registers
      5  Write Single Coil
      6  Write Single Register
     16  Write Multiple Registers
    """
    import math
    import socket
    import socketserver
    import struct
    import threading
    import time

    HOST = "0.0.0.0"
    PORT = 502
    UNIT_ID = 1

    # 16-bit register space and coil space (shared, guarded by a lock)
    _lock = threading.Lock()
    holding = [0] * 64
    coils = [False] * 16


    def _set_float(index, value):
        """Write a float32 into two consecutive registers, big-endian (ABCD)."""
        hi, lo = struct.unpack(">HH", struct.pack(">f", value))
        holding[index] = hi
        holding[index + 1] = lo


    def _set_int16(index, value):
        holding[index] = struct.unpack(">H", struct.pack(">h", int(value)))[0]


    def simulate():
        """Continuously update the register values so the data looks alive."""
        energy = 0.0
        t = 0.0
        while True:
            t += 1.0
            # smooth, bounded, obviously-synthetic process values
            temperature = 55.0 + 12.0 * math.sin(t / 30.0)
            pressure = 4.2 + 0.8 * math.sin(t / 17.0)
            flow = 120.0 + 25.0 * math.sin(t / 23.0)
            speed = 1500 + int(120 * math.sin(t / 11.0))
            energy += max(0.0, flow) * 0.0005

            # a fault trips briefly whenever pressure crosses the high limit
            fault = pressure > 4.9
            running = not fault
            state = 2 if fault else (1 if running else 0)

            with _lock:
                _set_float(0, temperature)
                _set_float(2, pressure)
                _set_float(4, flow)
                _set_float(6, energy)
                _set_int16(8, speed)
                _set_int16(9, state)
                coils[0] = running
                coils[1] = fault

            time.sleep(1.0)


    def _read_bits(source, address, count):
        payload = bytearray()
        byte_count = (count + 7) // 8
        payload.append(byte_count)
        chunk = bytearray(byte_count)
        for i in range(count):
            if address + i < len(source) and source[address + i]:
                chunk[i // 8] |= 1 << (i % 8)
        payload.extend(chunk)
        return bytes(payload)


    def _read_registers(source, address, count):
        payload = bytearray()
        payload.append(count * 2)
        for i in range(count):
            value = source[address + i] if address + i < len(source) else 0
            payload.extend(struct.pack(">H", value))
        return bytes(payload)


    def handle_pdu(pdu):
        """Return the response PDU for a request PDU (or an exception response)."""
        function = pdu[0]
        try:
            if function in (1, 2):
                address, count = struct.unpack(">HH", pdu[1:5])
                with _lock:
                    return bytes([function]) + _read_bits(coils, address, count)

            if function in (3, 4):
                address, count = struct.unpack(">HH", pdu[1:5])
                with _lock:
                    return bytes([function]) + _read_registers(holding, address, count)

            if function == 5:
                address, value = struct.unpack(">HH", pdu[1:5])
                with _lock:
                    if address < len(coils):
                        coils[address] = value == 0xFF00
                return pdu[:5]

            if function == 6:
                address, value = struct.unpack(">HH", pdu[1:5])
                with _lock:
                    if address < len(holding):
                        holding[address] = value
                return pdu[:5]

            if function == 16:
                address, count = struct.unpack(">HH", pdu[1:5])
                with _lock:
                    for i in range(count):
                        offset = 6 + i * 2
                        if address + i < len(holding):
                            holding[address + i] = struct.unpack(">H", pdu[offset:offset + 2])[0]
                return pdu[:1] + struct.pack(">HH", address, count)

            # illegal function
            return bytes([function | 0x80, 0x01])
        except Exception:
            # illegal data address / value
            return bytes([function | 0x80, 0x02])


    class ModbusHandler(socketserver.BaseRequestHandler):
        def handle(self):
            self.request.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)
            while True:
                header = self._recv_exact(7)
                if not header:
                    return
                transaction, protocol, length, unit = struct.unpack(">HHHB", header)
                body = self._recv_exact(length - 1)
                if body is None:
                    return
                response = handle_pdu(body)
                self.request.sendall(
                    struct.pack(">HHHB", transaction, protocol, len(response) + 1, unit) + response
                )

        def _recv_exact(self, count):
            buffer = b""
            while len(buffer) < count:
                chunk = self.request.recv(count - len(buffer))
                if not chunk:
                    return None
                buffer += chunk
            return buffer


    class ModbusServer(socketserver.ThreadingTCPServer):
        allow_reuse_address = True
        daemon_threads = True


    if __name__ == "__main__":
        threading.Thread(target=simulate, daemon=True).start()
        print(f"Modbus TCP simulator listening on {HOST}:{PORT} (unit id {UNIT_ID})", flush=True)
        ModbusServer((HOST, PORT), ModbusHandler).serve_forever()
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: modbus-simulator
  namespace: munich
spec:
  replicas: 1
  selector:
    matchLabels:
      app: modbus-simulator
  template:
    metadata:
      labels:
        app: modbus-simulator
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
        - name: modbus-simulator
          image: python:3.12-slim
          imagePullPolicy: IfNotPresent
          command: ["python3", "-u", "/app/modbus_server.py"]
          ports:
            - containerPort: 502
              name: modbus
          volumeMounts:
            - name: simulator
              mountPath: /app
          readinessProbe:
            tcpSocket:
              port: modbus
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: modbus
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
      volumes:
        - name: simulator
          configMap:
            name: modbus-simulator
            items:
              - key: modbus_server.py
                path: modbus_server.py
---
apiVersion: v1
kind: Service
metadata:
  name: modbus-simulator
  namespace: munich
spec:
  selector:
    app: modbus-simulator
  ports:
    - name: modbus
      port: 502
      targetPort: 502
---
# ---------------------------------------------------------------------------
# W3C WoT Thing Description for the simulated Modbus TCP device
#
# UA Edge Translator loads every *.jsonld file it finds in its settings folder
# at startup and onboards it as an OPC UA asset (one asset per file, named after
# the file). This TD therefore turns the Modbus simulator into a fully modelled
# OPC UA node set - without writing any code.
#
# 'base' addresses the in-cluster Modbus service; the trailing /1 is the Modbus
# unit id. Each property's form carries the Modbus binding:
#   href                        <start register>?quantity=<number of registers>
#   modv:entity                 HoldingRegister | InputRegister | Coil | DiscreteInput
#   modv:type                   xsd:float (4 bytes / 2 regs), xsd:short (2 bytes / 1 reg), xsd:boolean
#   modv:mostSignificantByte    true  => standard big-endian Modbus word order
#   modv:pollingTime            poll interval in milliseconds
# ---------------------------------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: modbus-thing-description
  namespace: edge
data:
  modbus-simulator.jsonld: |
    {
      "@context": [
        "https://www.w3.org/2022/wot/td/v1.1"
      ],
      "@type": [
        "Thing"
      ],
      "id": "urn:modbus-simulator",
      "name": "modbus-simulator",
      "title": "Munich Line Conditioning Unit (Modbus TCP simulation)",
      "description": "Simulated Modbus TCP device on the Munich production line, mapped into OPC UA by UA Edge Translator.",
      "securityDefinitions": {
        "nosec_sc": {
          "scheme": "nosec"
        }
      },
      "security": [
        "nosec_sc"
      ],
      "base": "modbus+tcp://modbus-simulator.munich.svc.cluster.local:502/1",
      "properties": {
        "Temperature": {
          "type": "number",
          "unit": "degreeCelsius",
          "description": "Process temperature",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "0?quantity=2",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "HoldingRegister",
              "modv:type": "xsd:float",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        },
        "Pressure": {
          "type": "number",
          "unit": "bar",
          "description": "Process pressure",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "2?quantity=2",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "HoldingRegister",
              "modv:type": "xsd:float",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        },
        "FlowRate": {
          "type": "number",
          "unit": "litrePerMinute",
          "description": "Coolant flow rate",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "4?quantity=2",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "HoldingRegister",
              "modv:type": "xsd:float",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        },
        "EnergyConsumption": {
          "type": "number",
          "unit": "kilowattHour",
          "description": "Cumulative energy consumption",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "6?quantity=2",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "HoldingRegister",
              "modv:type": "xsd:float",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 5000
            }
          ]
        },
        "MotorSpeed": {
          "type": "integer",
          "unit": "revolutionPerMinute",
          "description": "Motor speed",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "8?quantity=1",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "HoldingRegister",
              "modv:type": "xsd:short",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        },
        "MachineState": {
          "type": "integer",
          "description": "Machine state: 0 = stopped, 1 = running, 2 = fault",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "9?quantity=1",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "HoldingRegister",
              "modv:type": "xsd:short",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        },
        "Running": {
          "type": "boolean",
          "description": "True while the unit is running",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "0?quantity=1",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "Coil",
              "modv:type": "xsd:boolean",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        },
        "FaultActive": {
          "type": "boolean",
          "description": "True while a high-pressure fault is active",
          "readOnly": true,
          "observable": true,
          "forms": [
            {
              "href": "1?quantity=1",
              "op": [
                "readproperty",
                "observeproperty"
              ],
              "modv:entity": "Coil",
              "modv:type": "xsd:boolean",
              "modv:mostSignificantByte": true,
              "modv:pollingTime": 2000
            }
          ]
        }
      }
    }
---
# ---------------------------------------------------------------------------
# TLS termination for the edge web UIs
#
# UA Edge Translator and UA Cloud Publisher both authenticate with HTTP Basic,
# which sends reversible credentials on every request. Their Services are
# therefore ClusterIP, and this Ingress is the only way to reach them from
# outside - over HTTPS only.
#
# This is a SECOND Ingress, separate from the one in cloud.yaml, because a
# Kubernetes Ingress can only route to Services in its OWN namespace. It also
# keeps edge.yaml deployable on its own, which is the point of the edge/cloud
# split (see "Single Cluster vs. Split Edge/Cloud Deployment" in the README).
#
# Like the cloud services, routing is by HOSTNAME rather than path prefix:
# these apps emit root-relative asset URLs and do not call UsePathBase, so a
# path prefix would load the page but 404 every stylesheet and script.
#
# Requires an "edge-services-tls" Secret in the edge namespace. Kubernetes
# Secrets are namespace-scoped, so the certificate created for the cloud
# services has to be copied in - the README shows the one-line command.
#
# NOTE: only the HTTP user interfaces go through here. The OPC UA (4840),
# LoRaWAN (5000/5001) and OCPP (19520/19521) ports remain plain TCP on the node,
# because they are not HTTP and an HTTP ingress cannot carry them. OPC UA has
# its own transport security (see the GDS section in the README); LoRaWAN and
# OCPP have their own secure variants on 5001 and 19521.
#
# Reach the UIs at:
#   https://translator.plant.local    UA Edge Translator
#   https://publisher.plant.local     UA Cloud Publisher
# ---------------------------------------------------------------------------
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: edge-services
  namespace: edge
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - secretName: edge-services-tls
      hosts:
        - translator.plant.local
        - publisher.plant.local
  rules:
    - host: translator.plant.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ua-edgetranslator-ui
                port:
                  number: 8080
    - host: publisher.plant.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ua-cloudpublisher
                port:
                  number: 8081
