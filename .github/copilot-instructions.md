# Copilot Instructions

## Project Guidelines
- Never run `git commit` or `git push` on the user's behalf. Make changes in the working tree only, then report what changed and let the user review and check in the changes themselves.