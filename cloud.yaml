apiVersion: v1
kind: Namespace
metadata:
  name: cloud
---
apiVersion: v1
kind: Secret
metadata:
  name: influxdb-auth
  namespace: cloud
type: Opaque
stringData:
  INFLUX_TOKEN: "${INFLUX_TOKEN}"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: telegraf-conf
  namespace: cloud
data:
  telegraf.conf: |
    [global_tags]
      env = "dev"

    [agent]
      # mqtt_consumer is a SERVICE input: messages are pushed to the accumulator
      # asynchronously, so a 1s gather interval bought nothing and just produced
      # "Collection took longer than expected" spam once the Pi was loaded -
      # immediately before Telegraf began thrashing its own MQTT connection
      # (reconnecting every ~80s and delivering nothing).
      interval = "10s"
      round_interval = true
      metric_batch_size = 1000
      # 2000 was far too small: at 1Hz across ~58 writers any InfluxDB stall
      # overflowed the buffer and metrics were silently dropped.
      metric_buffer_limit = 100000
      collection_jitter = "0s"
      flush_interval = "5s"
      flush_jitter = "0s"
      logfile = ""
      # Do NOT tag every point with the Telegraf pod name. "host" is part of the
      # InfluxDB series key, and the pod name changes on every rollout, so leaving
      # this false forks a brand new series for ALL telemetry each time the pod is
      # replaced. That inflates series cardinality for no benefit and, worse, makes
      # last() return one row per historical pod unless the query groups them away,
      # so a panel can silently show a stale value from a retired pod's series.
      # The pod name is not a stable identity here - there is exactly one Telegraf
      # instance - so the tag carries no information worth keeping.
      omit_hostname = true

    ##################################################################################
    # MQTT â†’ consume OPC UA PubSub JSON data messages and parse it on ingest (json_v2)
    ##################################################################################
    [[inputs.mqtt_consumer]]
      servers = ["ssl://mosquitto.cloud.svc.cluster.local:8883"]
      topics  = ["data/#"]
      qos     = 1
      client_id = "telegraf-mqtt-data"
      username = "${IOT_USERNAME}"
      password = "${IOT_PASSWORD}"
      insecure_skip_verify = true
      connection_timeout = "30s"
      # Clean sessions (the old "false") meant the broker discarded anything
      # published while Telegraf was away - and Telegraf was reconnecting ~45
      # times an hour. With a persistent session the broker queues QoS-1
      # messages for this client_id instead, so restarts stop losing data.
      persistent_session = true

      # Keep the MQTT topic as a tag (useful for filtering/debugging)
      topic_tag = "mqtt_topic"

      # Parse payload as json_v2
      data_format = "json_v2"

      [[inputs.mqtt_consumer.json_v2]]
        # Measurement name for parsed points
        measurement_name = "opcua_pubsub"

        # Top-level PublisherId -> tag "publisher"
        [[inputs.mqtt_consumer.json_v2.tag]]
          path     = "PublisherId"
          rename   = "publisher"
          optional = false

        # NOTE: MessageId is deliberately NOT mapped here. It is unique for every
        # single message, so tagging it made InfluxDB create a brand new series
        # per message (~86k/day from this 1Hz input alone). That cardinality
        # explosion - not query shape or disk speed - is what made every Grafana
        # panel slow and drove InfluxDB's memory up. Tags must be low-cardinality.
        # If you ever need the id back, add it as a *field*, never as a tag.

        # Parse the array Messages[] into one metric per element
        [[inputs.mqtt_consumer.json_v2.object]]
          path = "Messages"

          # Use inner timestamp as point time (_time)  âœ…
          timestamp_key    = "Timestamp"
          timestamp_format = "2006-01-02T15:04:05.999999999Z07:00"

          # Keep nested prefixes so you get fields like Payload_ConnectedToBroker_Value
          disable_prepend_keys = false

          # Include DataSetWriterId as a TAG âœ…
          tags = ["DataSetWriterId"]

          [inputs.mqtt_consumer.json_v2.object.renames]
            DataSetWriterId = "datasetWriterId"

    ###########################################################################
    # UA-METADATA messages (ua-metadata)
    #
    # One message describes metadata for a given DataSetWriterId.
    # We'll store it as measurement "opcua_metadata".
    #
    # We:
    #   - use top-level Timestamp as _time via timestamp_path
    #   - keep PublisherId + DataSetWriterId as tags
    #   - extract MetaData.Name and first Field details (Fields[0].*)
    ###########################################################################
    [[inputs.mqtt_consumer]]
      servers = ["ssl://mosquitto.cloud.svc.cluster.local:8883"]
      topics  = ["metadata"]
      qos     = 1
      client_id = "telegraf-mqtt-metadata"
      username = "${IOT_USERNAME}"
      password = "${IOT_PASSWORD}"
      insecure_skip_verify = true
      connection_timeout = "30s"
      # See the note on the data input above.
      persistent_session = true
      topic_tag = "mqtt_topic"

      data_format = "json_v2"

      [[inputs.mqtt_consumer.json_v2]]
        measurement_name = "opcua_metadata"

        # Use the inner Timestamp as point time (_time)
        timestamp_path    = "Timestamp"
        timestamp_format  = "2006-01-02T15:04:05.999999999Z07:00"

        # Tags
        [[inputs.mqtt_consumer.json_v2.tag]]
          path     = "PublisherId"
          rename   = "publisher"
          optional = false

        [[inputs.mqtt_consumer.json_v2.tag]]
          path     = "DataSetWriterId"
          rename   = "datasetWriterId"
          # The metadata topic carries more than ua-metadata: the Publisher also
          # sends ua-status (every DiagnosticsLoggingInterval) and ua-connection
          # messages through the same sink (SendMetadataAsync in
          # MessageProcessor.cs). Those describe the Publisher itself, not a
          # DataSetWriter, so they have no top-level DataSetWriterId and a
          # mandatory path made json_v2 reject the whole message with
          #   Error in plugin: the path "DataSetWriterId" doesn't exist
          # once per message. Marking it optional lets the ua-metadata messages
          # parse normally; the status/connection messages then contribute no
          # tag, and are dropped by the filter below.
          optional = true

        # NOTE: MessageId is deliberately NOT mapped here - see the comment in the
        # data input above. It is unique per message and tagging it created one
        # InfluxDB series per metadata message, which is why scanning
        # opcua_metadata (87k points over 2 days) was so expensive in every panel.

        # Metadata name (this can be long; tag is convenient for filtering,
        # but if you worry about cardinality, switch this to a field below.)
        [[inputs.mqtt_consumer.json_v2.tag]]
          path     = "MetaData.Name"
          rename   = "metaName"
          optional = true

        # Fields[0] information (first field in the metadata Fields array)
        [[inputs.mqtt_consumer.json_v2.tag]]
          path     = "MetaData.Fields.0.Name"
          rename   = "fieldName"
          optional = true

        [[inputs.mqtt_consumer.json_v2.tag]]
          path     = "MetaData.Fields.0.Description"
          rename   = "fieldDescription"
          optional = true

        # Store these as numeric fields
        [[inputs.mqtt_consumer.json_v2.field]]
          path     = "MetaData.Fields.0.BuiltInType"
          rename   = "builtInType"
          type     = "int"
          optional = true

        [[inputs.mqtt_consumer.json_v2.field]]
          path     = "MetaData.Fields.0.DataType.Id"
          rename   = "dataTypeId"
          type     = "int"
          optional = true

        [[inputs.mqtt_consumer.json_v2.field]]
          path     = "MetaData.Fields.0.ValueRank"
          rename   = "valueRank"
          type     = "int"
          optional = true

        [[inputs.mqtt_consumer.json_v2.field]]
          path     = "MetaData.Fields.0.FieldFlags"
          rename   = "fieldFlags"
          type     = "int"
          optional = true

        # ConfigurationVersion (useful for detecting schema changes)
        [[inputs.mqtt_consumer.json_v2.field]]
          path     = "MetaData.ConfigurationVersion.MajorVersion"
          rename   = "cfgMajor"
          type     = "int"
          optional = true

        [[inputs.mqtt_consumer.json_v2.field]]
          path     = "MetaData.ConfigurationVersion.MinorVersion"
          rename   = "cfgMinor"
          type     = "int"
          optional = true

    ###########################################################################
    # Drop the ua-status / ua-connection messages that share the metadata topic.
    #
    # They describe the Publisher itself rather than a DataSetWriter, so they
    # produce a point with no datasetWriterId and no MetaData.* values - nothing
    # the dashboards or UA Cloud Action can join on. Writing them would add
    # useless series to the opcua_metadata measurement and make the writer
    # lookups scan more points than necessary.
    #
    # ua-metadata messages always carry a datasetWriterId tag; status and
    # connection messages do not, so requiring the tag selects exactly the
    # metadata messages.
    ###########################################################################
    [[processors.filter]]
      namepass = ["opcua_metadata"]
      order = 1

      # A rule's tag criteria require the key to EXIST (and one value to match),
      # so this cannot be written as a "drop metrics without the tag" rule.
      # Instead: pass anything that has a datasetWriterId, and default-drop the
      # rest - which is exactly the ua-status / ua-connection messages.
      default = "drop"

      [[processors.filter.rule]]
        action = "pass"
        tags = { datasetWriterId = ["*"] }

    ###########################################################################
    # Output â†’ InfluxDB v2
    ###########################################################################
    [[outputs.influxdb_v2]]
      urls = ["http://influxdb.cloud.svc.cluster.local:8086"]
      token = "$INFLUX_TOKEN"
      organization = "iot"
      bucket = "mqtt"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-conf
  namespace: cloud
data:
  mosquitto.conf: |
    pid_file /mosquitto/mosquitto.pid
    persistence true
    persistence_location /mosquitto/data/
    log_dest stdout
    allow_anonymous false
    password_file /mosquitto/data/passwd
    listener 8883
    cafile /mosquitto/certs/ca.crt
    certfile /mosquitto/certs/server.crt
    keyfile /mosquitto/certs/server.key
    # Mosquitto defaults max_keepalive to 60s and, under MQTT 5.0, silently
    # overrides any larger client request via the CONNACK "Server Keep Alive"
    # property. UA Cloud Publisher asks for 100s (see MQTTClient.cs) and keeps
    # pinging on that schedule, so the broker stops seeing PINGREQ in time and
    # evicts the session with a clean "NormalDisconnection" every ~30s. Allow the
    # longer interval so long-lived publisher sessions are not torn down.
    max_keepalive 120
    # Telegraf now uses persistent sessions, so the broker queues QoS-1 messages
    # for it while it is disconnected. The default max_queued_messages of 1000 is
    # only ~16s of traffic at this rate, which would make a restart lossy anyway.
    max_queued_messages 200000
    # TLS + username/password authentication are enabled. For production,
    # replace the self-signed certificates with ones from a trusted CA.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mosquitto
  namespace: cloud
spec:
  replicas: 1
  # Single instance with a hostPath volume: the default RollingUpdate strategy
  # starts the replacement pod BEFORE terminating the old one, so both briefly
  # share the same host directory. Recreate terminates first, then starts.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mosquitto
  template:
    metadata:
      labels:
        app: mosquitto
    spec:
      initContainers:
        - name: init-chown-data
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c", "chown -R 1883:1883 /mosquitto/data"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: mosquitto-data
              mountPath: /mosquitto/data
        - name: gen-certs
          image: alpine/openssl:3.5.7
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              cd /certs
              # IDEMPOTENT: only generate if we do not already have a cert. These
              # files live on a hostPath, so they now survive pod restarts.
              # Previously this ran unconditionally against an emptyDir, so EVERY
              # broker restart minted a brand new CA and server certificate and
              # handed every client a different identity - tearing down all
              # established TLS sessions at once and showing up in the clients as
              # "Disconnected from MQTT broker" with assorted reason codes.
              if [ -s ca.crt ] && [ -s server.crt ] && [ -s server.key ]; then
                echo "Existing broker certificates found; reusing them."
                exit 0
              fi
              echo "No broker certificates present; generating a new set."
              # Self-signed CA
              openssl req -new -x509 -days 3650 -nodes \
                -subj "/CN=Mosquitto Demo CA" \
                -keyout ca.key -out ca.crt
              # Server key + CSR
              openssl genrsa -out server.key 2048
              openssl req -new -key server.key \
                -subj "/CN=mosquitto.cloud.svc.cluster.local" \
                -out server.csr
              # Sign the server cert with SANs for in-cluster and local access
              printf "subjectAltName=DNS:mosquitto,DNS:mosquitto.cloud,DNS:mosquitto.cloud.svc,DNS:mosquitto.cloud.svc.cluster.local,DNS:localhost,IP:127.0.0.1\n" > san.cnf
              openssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key \
                -CAcreateserial -days 3650 -out server.crt -extfile san.cnf
              chmod 644 ca.crt server.crt server.key
          volumeMounts:
            - name: mosquitto-certs
              mountPath: /certs
      containers:
        - name: mosquitto
          image: eclipse-mosquitto:2.1.2-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: MOSQUITTO_USERNAME
              value: "${IOT_USERNAME}"
            - name: MOSQUITTO_PASSWORD
              value: "${IOT_PASSWORD}"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              # Generate the password file, then hand it to the uid the broker drops
              # privileges to. mosquitto.conf has no "user" directive, so Mosquitto
              # switches to the "mosquitto" user (uid/gid 1883) AFTER reading the config
              # and re-opens password_file as that user. mosquitto_passwd creates the
              # file 0600 owned by whoever runs this script, so without the chown/chmod
              # below the broker fails with:
              #   Error: Unable to open pwfile "/mosquitto/data/passwd".
              #
              # Mosquitto 2.1 CHANGED mosquitto_passwd -c: it now refuses to overwrite
              # an existing file, failing with
              #   Error: Unable to open file /mosquitto/data/passwd for writing. File exists.
              # whereas 2.0 replaced it silently. /mosquitto is a hostPath, so the file
              # survives pod restarts - delete it first so the credentials are always
              # regenerated from MOSQUITTO_USERNAME/MOSQUITTO_PASSWORD and stay in sync
              # with whatever was supplied at apply time.
              rm -f /mosquitto/data/passwd
              mosquitto_passwd -b -c /mosquitto/data/passwd "$MOSQUITTO_USERNAME" "$MOSQUITTO_PASSWORD"
              chown 1883:1883 /mosquitto/data/passwd || true
              chmod 0640 /mosquitto/data/passwd
              exec /usr/sbin/mosquitto -c /mosquitto/config/mosquitto.conf
          ports:
            - containerPort: 8883
              name: mqtts
          volumeMounts:
            - name: mosquitto-conf
              mountPath: /mosquitto/config
            - name: mosquitto-data
              mountPath: /mosquitto/data
            - name: mosquitto-certs
              mountPath: /mosquitto/certs
          # NOTE: deliberately NOT a tcpSocket probe on 8883. kubelet would open a
          # raw TCP connection and close it immediately without completing the TLS
          # handshake, which Mosquitto logs as
          #   OpenSSL Error ... unexpected eof while reading
          #   Client <ip> disconnected: Protocol error.
          # With liveness + readiness both probing, that is one such pair every ~5s,
          # which floods the log and hides real client problems. Checking that the
          # broker process is alive gives the same signal without opening a socket.
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep mosquitto > /dev/null"]
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep mosquitto > /dev/null"]
            initialDelaySeconds: 5
            periodSeconds: 30
      volumes:
        - name: mosquitto-conf
          configMap:
            name: mosquitto-conf
            items:
              - key: mosquitto.conf
                path: mosquitto.conf
        - name: mosquitto-data
          hostPath:
            path: "/mosquitto"
        - name: mosquitto-certs
          # hostPath (not emptyDir) so the broker keeps the SAME TLS identity
          # across restarts. With an emptyDir every restart produced a new CA and
          # server cert, disconnecting every MQTT client simultaneously.
          hostPath:
            path: "/mosquitto/certs"
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: mosquitto
  ports:
    - name: mqtts
      port: 8883
      targetPort: 8883
---
# ---------------------------------------------------------------------------
# MQTT Explorer - the web UI for the Mosquitto broker.
#
# Mosquitto itself has no user interface, so this provides one: browse the live
# topic tree, inspect the OPC UA PubSub payloads flowing on data/# and metadata,
# and hand-craft messages - most usefully for exercising UA Cloud Commander on
# the commands/responses topics without needing mosquitto_pub on a terminal.
#
# The broker connection is pre-seeded via INITIAL_CONFIG (see the ConfigMap
# below), so the Mosquitto host, port, credentials and topic subscriptions are
# already filled in when the UI first loads.
#
# SECURITY: unlike the other UIs in this stack, MQTT Explorer has NO built-in
# authentication, and anyone who reaches it can publish arbitrary messages to the
# broker - including commands/# which UA Cloud Commander will execute against
# your OPC UA servers. Keep it on a trusted network, put it behind an
# authenticating reverse proxy, or scale it to 0 when not in use:
#     kubectl scale deployment/mqtt-explorer -n cloud --replicas=0
# ---------------------------------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: mqtt-explorer-config
  namespace: cloud
data:
  # Seeds MQTT Explorer's lowdb settings store. The key and shape are taken from
  # the image's own source: ConfigStorage writes <CONFIG_PATH>/settings.json, and
  # ConnectionManager.ts persists the connection dictionary under the storage id
  # "ConnectionManager_connections"; each entry is a ConnectionOptions record
  # (app/src/model/ConnectionOptions.ts).
  #
  # certValidation is false because the broker presents the self-signed
  # certificate generated by the gen-certs init container - the same reason
  # Telegraf sets insecure_skip_verify and UA Cloud Action sets
  # MQTT_TLS_INSECURE.
  settings.json: |
    {
      "ConnectionManager_connections": {
        "mosquitto": {
          "configVersion": 1,
          "type": "mqtt",
          "id": "mosquitto",
          "name": "Mosquitto (this cluster)",
          "host": "mosquitto.cloud.svc.cluster.local",
          "port": 8883,
          "protocol": "mqtt",
          "encryption": true,
          "certValidation": false,
          "username": "${IOT_USERNAME}",
          "password": "${IOT_PASSWORD}",
          "clientId": "mqtt-explorer",
          "subscriptions": [
            {
              "topic": "#",
              "qos": 0
            }
          ]
        }
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mqtt-explorer
  namespace: cloud
spec:
  replicas: 1
  # Single instance; terminate before starting the replacement, matching the
  # convention used by the rest of this stack.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mqtt-explorer
  template:
    metadata:
      labels:
        app: mqtt-explorer
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      initContainers:
        # CONFIG_PATH must be writable (the app rewrites settings.json whenever a
        # connection is edited in the UI), so the seed is copied out of the
        # read-only ConfigMap mount into an emptyDir.
        - name: seed-config
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              if [ -s /config/settings.json ]; then
                echo "settings.json already present; leaving user edits intact."
                exit 0
              fi
              cp /seed/settings.json /config/settings.json
              echo "Seeded MQTT Explorer with the Mosquitto connection."
          volumeMounts:
            - name: seed
              mountPath: /seed
            - name: config
              mountPath: /config
      containers:
        - name: mqtt-explorer
          image: smeagolworms4/mqtt-explorer:browser-1.0.3
          imagePullPolicy: IfNotPresent
          env:
            - name: HTTP_PORT
              value: "4000"
            - name: CONFIG_PATH
              value: "/mqtt-explorer/config"
          volumeMounts:
            - name: config
              mountPath: /mqtt-explorer/config
          ports:
            - containerPort: 4000
              name: http
          # Modest footprint so the UI can never starve the data pipeline.
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              memory: "256Mi"
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: seed
          configMap:
            name: mqtt-explorer-config
        # Writable copy: the app rewrites settings.json when connections are
        # edited in the UI. Edits are intentionally not persisted across restarts
        # so the pod always comes back with the provisioned connection.
        - name: config
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mqtt-explorer
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: mqtt-explorer
  ports:
    - name: http
      port: 4000
      targetPort: 4000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: influxdb
  namespace: cloud
spec:
  replicas: 1
  # InfluxDB is a single instance backed by the hostPath /influxdb2 and takes an
  # exclusive lock on its engine directory. The default RollingUpdate strategy
  # starts the new pod BEFORE terminating the old one, so the new pod can never
  # acquire the lock: it CrashLoopBackOffs and the rollout deadlocks forever
  # while two pods fight over the same data directory. Recreate terminates the
  # old pod first, which is the only safe option for a single-writer database on
  # a hostPath volume.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: influxdb
  template:
    metadata:
      labels:
        app: influxdb
    spec:
      containers:
        - name: influxdb
          image: influxdb:2.9
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
              name: http
          env:
            # Optional first-run setup via env vars (if not using the UI)
            - name: INFLUXD_REPORTING_DISABLED
              value: "true"
            - name: DOCKER_INFLUXDB_INIT_MODE
              value: "setup"
            - name: DOCKER_INFLUXDB_INIT_USERNAME
              value: "${IOT_USERNAME}"
            - name: DOCKER_INFLUXDB_INIT_PASSWORD
              value: "${IOT_PASSWORD}"
            - name: DOCKER_INFLUXDB_INIT_ORG
              value: "iot"
            - name: DOCKER_INFLUXDB_INIT_BUCKET
              value: "mqtt"
            # Bound the bucket's lifetime. Without this the bucket is created with
            # INFINITE retention, so nothing ever ages out - which meant the series
            # created by the old messageId tag (one series per message) would have
            # polluted the index forever. NOTE: this only applies when InfluxDB
            # first initialises (INIT_MODE=setup); it does NOT alter an existing
            # bucket. To change retention on a live bucket use:
            #   influx bucket update --name mqtt --org iot --retention 30d
            - name: DOCKER_INFLUXDB_INIT_RETENTION
              value: "30d"
            - name: DOCKER_INFLUXDB_INIT_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: influxdb-auth
                  key: INFLUX_TOKEN
          volumeMounts:
            - name: data
              mountPath: /var/lib/influxdb2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: "data"
          hostPath:
            path: "/influxdb2"
---
apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: influxdb
  ports:
    - name: http
      port: 8086
      targetPort: 8086
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegraf
  namespace: cloud
spec:
  replicas: 1
  # Both mqtt_consumer inputs use a FIXED client_id (telegraf-mqtt-data /
  # telegraf-mqtt-metadata). MQTT allows only one session per client id, so the
  # broker disconnects the older client the moment a second one connects with
  # the same id. The default RollingUpdate strategy starts the replacement pod
  # BEFORE terminating the old one, so during every rollout two pods fight over
  # the same two client ids and each kicks the other off in a loop - visible as
  #   Connected [ssl://mosquitto...] / Error in plugin: connection lost: EOF
  # repeating every few seconds, with no data reaching InfluxDB.
  # Recreate terminates the old pod first, so the ids are never contended.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: telegraf
  template:
    metadata:
      labels:
        app: telegraf
    spec:
      initContainers:
        - name: wait-for-mosquitto
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "Waiting for mosquitto:8883 (max 300s) ..."
              timeout 300 sh -c 'until nc -z -w 2 mosquitto 8883; do sleep 5; done' \
                && echo "Mosquitto is up" \
                || echo "WARNING: mosquitto:8883 not reachable - starting anyway"
              exit 0
      containers:
        - name: telegraf
          image: telegraf:1.39-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: INFLUX_TOKEN
              valueFrom:
                secretKeyRef:
                  name: influxdb-auth
                  key: INFLUX_TOKEN
          volumeMounts:
            - name: telegraf-conf
              mountPath: /etc/telegraf
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep telegraf >/dev/null || exit 1"]
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "telegraf --version >/dev/null 2>&1"]
            initialDelaySeconds: 5
            periodSeconds: 20
      volumes:
        - name: telegraf-conf
          configMap:
            name: telegraf-conf
            items:
              - key: telegraf.conf
                path: telegraf.conf
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: portainer-sa-clusteradmin
  namespace: cloud
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: portainer-crb-clusteradmin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: portainer-sa-clusteradmin
  namespace: cloud
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portainer
  namespace: cloud
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: portainer
  template:
    metadata:
      labels:
        app: portainer
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      # Grants Portainer in-cluster access to the K3s API server to manage the cluster
      serviceAccountName: portainer-sa-clusteradmin
      containers:
      - name: portainer
        image: portainer/portainer-ce:2.44.0
        imagePullPolicy: IfNotPresent
        args:
        # Use the in-cluster (Kubernetes) environment as the default endpoint
        - "--tunnel-port=8000"
        # Newer Portainer releases (this appeared after 2.21) refuse to show the
        # "create admin user" page until you paste a setup token that is printed
        # to the container log, as protection against someone else claiming a
        # publicly reachable instance first. That is a poor fit here: the UI is
        # deployed unattended as part of the stack, so the first thing a user
        # would have to do is go and read the pod log.
        #
        # SECURITY: this restores the older behaviour where whoever reaches the
        # UI first creates the admin account. Browse to it and set the password
        # promptly after deploying - see "Managing the Cluster with Portainer"
        # in the README.
        - "--no-setup-token"
        ports:
        - name: http
          containerPort: 9000
        - name: https
          containerPort: 9443
        - name: tunnel
          containerPort: 8000
        volumeMounts:
        - name: data
          mountPath: /data
        livenessProbe:
          httpGet:
            path: /
            port: 9443
            scheme: HTTPS
          initialDelaySeconds: 20
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /
            port: 9443
            scheme: HTTPS
          initialDelaySeconds: 10
          periodSeconds: 20
      volumes:
        - name: data
          hostPath:
            path: "/portainer"
---
apiVersion: v1
kind: Service
metadata:
  name: portainer
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: portainer
  ports:
    - name: https
      port: 9443
      targetPort: 9443
    - name: http
      port: 9000
      targetPort: 9000
    - name: tunnel
      port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: cloud
data:
  datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: InfluxDB
        # The uid is referenced by every provisioned dashboard panel
        # ("datasource": { "type": "influxdb", "uid": "influxdb" }). Without an
        # explicit uid Grafana generates a random one and the panels resolve to a
        # non-existent datasource, so they render empty with no query ever issued.
        uid: influxdb
        type: influxdb
        access: proxy
        url: http://influxdb.cloud.svc.cluster.local:8086
        isDefault: true
        jsonData:
          version: Flux
          organization: iot
          defaultBucket: mqtt
          tlsSkipVerify: true
        secureJsonData:
          # Interpolated from the INFLUX_TOKEN environment variable at startup
          token: $INFLUX_TOKEN
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
  namespace: cloud
data:
  provider.yaml: |
    apiVersion: 1
    providers:
      - name: default
        orgId: 1
        folder: ""
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: cloud
data:
  production-line-oee.json: |
    {
      "annotations": {
        "list": [
        ]
      },
      "editable": true,
      "graphTooltip": 1,
      "schemaVersion": 41,
      "title": "Production Line OEE",
      "uid": "production-line-oee",
      "tags": [
        "opcua",
        "oee",
        "manufacturing"
      ],
      "time": {
        "from": "now-6h",
        "to": "now"
      },
      "refresh": "30s",
      "templating": {
        "list": [
          {
            "name": "station",
            "label": "Station",
            "type": "custom",
            "query": "assembly,test,packaging",
            "current": {
              "text": "assembly",
              "value": "assembly",
              "selected": true
            },
            "options": [
              {
                "text": "assembly",
                "value": "assembly",
                "selected": true
              },
              {
                "text": "test",
                "value": "test",
                "selected": false
              },
              {
                "text": "packaging",
                "value": "packaging",
                "selected": false
              }
            ],
            "includeAll": false,
            "multi": false
          }
        ]
      },
      "panels": [
        {
          "id": 100,
          "title": "OEE - production line (bottleneck)",
          "type": "gauge",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 0,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "unit": "percent",
              "min": 0,
              "max": 100,
              "decimals": 1,
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "red",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 50
                  },
                  {
                    "color": "green",
                    "value": 75
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            },
            "showThresholdLabels": false,
            "showThresholdMarkers": true
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"array\"\nimport \"regexp\"\n\n// ---------------------------------------------------------------------------\n// Line OEE = the OEE of the BOTTLENECK station, not the mean and not the pooled\n// aggregate. This mirrors CalculateOEEForLine() in the Manufacturing Ontologies\n// reference (Tools/FabricQueries/opcua_setup.kql), which evaluates\n// CalculateOEEForStation() per workcell and then takes summarize min(oee).\n//\n// The rationale is physical: on a serial line (assembly -\u003e test -\u003e packaging)\n// the stations are coupled, so the slowest station starves everything\n// downstream and blocks everything upstream. Line throughput is therefore\n// governed by the constraint, and averaging would hide the very station you\n// need to act on.\n//\n// The reference also excludes the publisher/mes/commander workcells; here the\n// station list is explicit for the same reason.\n// ---------------------------------------------------------------------------\nidealCycleTimeMs = 6000.0\nbucket = \"mqtt\"\n\nstart = v.timeRangeStart\nstop  = v.timeRangeStop\n\n// Window length in ms. int(v: time) is nanoseconds since the epoch.\nidealRunningTimeMs = float(v: int(v: stop) - int(v: start)) / 1000000.0\n\noeeFor = (station) =\u003e {\n\n  // DataSetWriterIds belonging to this station, taken from the metadata stream.\n  // Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n  // deliberately generous: it is effectively how long the publisher may stop\n  // publishing metadata before every panel goes blank. Do not shrink it without\n  // considering that trade-off.\n  // PERF: metaName is a tag, so a regex predicate is pushed into the storage\n  // engine; strings.containsStr() was evaluated row-by-row over every metadata\n  // point in the lookback window.\n  stationRx = regexp.compile(v: station)\n\n  writers =\n    from(bucket: bucket)\n    |\u003e range(start: -2d)\n    |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n    |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n    |\u003e keep(columns: [\"datasetWriterId\"])\n    |\u003e group()\n    |\u003e distinct(column: \"datasetWriterId\")\n    |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n  // PERF: a regex predicate on a tag can be pushed into the storage engine,\n  // whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\n  writersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\n  producedField = \"Payload_NumberOfManufacturedProducts_Value\"\n  scrappedField = \"Payload_NumberOfDiscardedProducts_Value\"\n  faultyField   = \"Payload_FaultyTime_Value\"\n\n  // PERF: every findRecord() call re-executes its entire upstream pipeline. The\n  // previous version grouped by _field and called findRecord() three times, so the\n  // window was scanned three times - the query plan showed three identical\n  // ReadRange branches and the query took 56s (Grafana times out at 30s).\n  // Reducing all three fields into a SINGLE row scans the window once and needs\n  // only one findRecord().\n  raw =\n    from(bucket: bucket)\n    |\u003e range(start: start, stop: stop)\n    |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\")\n    |\u003e filter(fn: (r) =\u003e r._field == producedField or r._field == scrappedField or r._field == faultyField)\n    |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n    |\u003e map(fn: (r) =\u003e ({_field: r._field, _value: float(v: r._value), seed: 0.0}))\n    |\u003e keep(columns: [\"_field\", \"_value\", \"seed\"])\n\n  // One flagged seed row keeps the table non-empty so findRecord() always returns a\n  // record (FaultyTime does not exist until the first fault occurs). It is flagged\n  // so it cannot influence any aggregate.\n  seed = array.from(rows: [{_field: \"seed\", _value: 0.0, seed: 1.0}])\n\n  stat =\n    union(tables: [raw, seed])\n    |\u003e group()\n    |\u003e reduce(\n      identity: {pMin: 0.0, pMax: 0.0, pN: 0.0, sMin: 0.0, sMax: 0.0, sN: 0.0, fSum: 0.0},\n      fn: (r, accumulator) =\u003e ({\n        pMin: if r.seed \u003e 0.0 or r._field != producedField then accumulator.pMin\n          else if accumulator.pN == 0.0 or r._value \u003c accumulator.pMin then r._value\n          else accumulator.pMin,\n        pMax: if r.seed \u003e 0.0 or r._field != producedField then accumulator.pMax\n          else if accumulator.pN == 0.0 or r._value \u003e accumulator.pMax then r._value\n          else accumulator.pMax,\n        pN:   if r.seed \u003e 0.0 or r._field != producedField then accumulator.pN\n          else accumulator.pN + 1.0,\n        sMin: if r.seed \u003e 0.0 or r._field != scrappedField then accumulator.sMin\n          else if accumulator.sN == 0.0 or r._value \u003c accumulator.sMin then r._value\n          else accumulator.sMin,\n        sMax: if r.seed \u003e 0.0 or r._field != scrappedField then accumulator.sMax\n          else if accumulator.sN == 0.0 or r._value \u003e accumulator.sMax then r._value\n          else accumulator.sMax,\n        sN:   if r.seed \u003e 0.0 or r._field != scrappedField then accumulator.sN\n          else accumulator.sN + 1.0,\n        fSum: if r.seed \u003e 0.0 or r._field != faultyField then accumulator.fSum\n          else accumulator.fSum + r._value,\n      }),\n    )\n    |\u003e findRecord(fn: (key) =\u003e true, idx: 0)\n\n  // Counters are cumulative, so the increase over the window is max - min.\n  produced = stat.pMax - stat.pMin\n  scrapped = stat.sMax - stat.sMin\n\n  // FaultyTime spikes once per fault with that fault duration, so it is summed.\n  faultyMs = stat.fSum\n\n  runTimeMs   = idealRunningTimeMs - faultyMs\n  totalPieces = produced + scrapped\n\n  availability =\n    if idealRunningTimeMs \u003e 0.0 and runTimeMs \u003e 0.0 then runTimeMs / idealRunningTimeMs\n    else 0.0\n\n  performance =\n    if runTimeMs \u003e 0.0 and totalPieces \u003e 0.0 then idealCycleTimeMs * totalPieces / runTimeMs\n    else 0.0\n\n  quality =\n    if totalPieces \u003e 0.0 then produced / totalPieces\n    else 0.0\n\n  // The simulated stations start at half their nominal cycle time\n  // (m_actualCycleTime = CycleTime * 500), so Performance legitimately exceeds 1.0\n  // and would push the gauge past 100%. Clamp here so the gauge stays in range.\n  perfClamped = if performance \u003e 1.0 then 1.0 else performance\n\n\n  return availability * perfClamped * quality * 100.0\n}\n\n// One evaluation per station on the line.\nassemblyOee  = oeeFor(station: \"assembly\")\ntestOee      = oeeFor(station: \"test\")\npackagingOee = oeeFor(station: \"packaging\")\n\n// Bottleneck: the minimum across the line.\nminAT   = if assemblyOee \u003c testOee then assemblyOee else testOee\nlineOee = if minAT \u003c packagingOee then minAT else packagingOee\n\narray.from(\n  rows: [\n  {\n\t_time: stop,\n\t_field: \"OEE\",\n\t_value: lineOee,\n  },\n  ],\n)"
            }
          ]
        },
        {
          "id": 10,
          "title": "OEE - $station",
          "type": "gauge",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 12,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "unit": "percent",
              "min": 0,
              "max": 100,
              "decimals": 1,
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "red",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 50
                  },
                  {
                    "color": "green",
                    "value": 75
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            },
            "showThresholdLabels": false,
            "showThresholdMarkers": true
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"array\"\nimport \"regexp\"\n\nstation = \"${station}\"\nidealCycleTimeMs = 6000.0\nbucket = \"mqtt\"\n\nstart = v.timeRangeStart\nstop  = v.timeRangeStop\n\n// Window length in ms. int(v: time) is nanoseconds since the epoch.\nidealRunningTimeMs = float(v: int(v: stop) - int(v: start)) / 1000000.0\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: bucket)\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nproducedField = \"Payload_NumberOfManufacturedProducts_Value\"\nscrappedField = \"Payload_NumberOfDiscardedProducts_Value\"\nfaultyField   = \"Payload_FaultyTime_Value\"\n\n// PERF: every findRecord() call re-executes its entire upstream pipeline. The\n// previous version grouped by _field and called findRecord() three times, so the\n// window was scanned three times - the query plan showed three identical\n// ReadRange branches and the query took 56s (Grafana times out at 30s).\n// Reducing all three fields into a SINGLE row scans the window once and needs\n// only one findRecord().\nraw =\n  from(bucket: bucket)\n  |\u003e range(start: start, stop: stop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\")\n  |\u003e filter(fn: (r) =\u003e r._field == producedField or r._field == scrappedField or r._field == faultyField)\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({_field: r._field, _value: float(v: r._value), seed: 0.0}))\n  |\u003e keep(columns: [\"_field\", \"_value\", \"seed\"])\n\n// One flagged seed row keeps the table non-empty so findRecord() always returns a\n// record (FaultyTime does not exist until the first fault occurs). It is flagged\n// so it cannot influence any aggregate.\nseed = array.from(rows: [{_field: \"seed\", _value: 0.0, seed: 1.0}])\n\nstat =\n  union(tables: [raw, seed])\n  |\u003e group()\n  |\u003e reduce(\n    identity: {pMin: 0.0, pMax: 0.0, pN: 0.0, sMin: 0.0, sMax: 0.0, sN: 0.0, fSum: 0.0},\n    fn: (r, accumulator) =\u003e ({\n      pMin: if r.seed \u003e 0.0 or r._field != producedField then accumulator.pMin\n        else if accumulator.pN == 0.0 or r._value \u003c accumulator.pMin then r._value\n        else accumulator.pMin,\n      pMax: if r.seed \u003e 0.0 or r._field != producedField then accumulator.pMax\n        else if accumulator.pN == 0.0 or r._value \u003e accumulator.pMax then r._value\n        else accumulator.pMax,\n      pN:   if r.seed \u003e 0.0 or r._field != producedField then accumulator.pN\n        else accumulator.pN + 1.0,\n      sMin: if r.seed \u003e 0.0 or r._field != scrappedField then accumulator.sMin\n        else if accumulator.sN == 0.0 or r._value \u003c accumulator.sMin then r._value\n        else accumulator.sMin,\n      sMax: if r.seed \u003e 0.0 or r._field != scrappedField then accumulator.sMax\n        else if accumulator.sN == 0.0 or r._value \u003e accumulator.sMax then r._value\n        else accumulator.sMax,\n      sN:   if r.seed \u003e 0.0 or r._field != scrappedField then accumulator.sN\n        else accumulator.sN + 1.0,\n      fSum: if r.seed \u003e 0.0 or r._field != faultyField then accumulator.fSum\n        else accumulator.fSum + r._value,\n    }),\n  )\n  |\u003e findRecord(fn: (key) =\u003e true, idx: 0)\n\n// Counters are cumulative, so the increase over the window is max - min.\nproduced = stat.pMax - stat.pMin\nscrapped = stat.sMax - stat.sMin\n\n// FaultyTime spikes once per fault with that fault duration, so it is summed.\nfaultyMs = stat.fSum\n\nrunTimeMs   = idealRunningTimeMs - faultyMs\ntotalPieces = produced + scrapped\n\navailability =\n  if idealRunningTimeMs \u003e 0.0 and runTimeMs \u003e 0.0 then runTimeMs / idealRunningTimeMs\n  else 0.0\n\nperformance =\n  if runTimeMs \u003e 0.0 and totalPieces \u003e 0.0 then idealCycleTimeMs * totalPieces / runTimeMs\n  else 0.0\n\nquality =\n  if totalPieces \u003e 0.0 then produced / totalPieces\n  else 0.0\n\n// The simulated stations start at half their nominal cycle time\n// (m_actualCycleTime = CycleTime * 500), so Performance legitimately exceeds 1.0\n// and would push the gauge past 100%. Clamp here so the gauge stays in range.\nperfClamped = if performance \u003e 1.0 then 1.0 else performance\n\narray.from(\n  rows: [\n  {\n    _time: stop,\n    _field: \"OEE\",\n    _value: availability * perfClamped * quality * 100.0,\n  },\n  ],\n)"
            }
          ]
        },
        {
          "id": 40,
          "title": "Status - $station",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 8
          },
          "fieldConfig": {
            "defaults": {
              "mappings": [
                {
                  "type": "value",
                  "options": {
                    "0": {
                      "text": "Ready",
                      "color": "blue",
                      "index": 0
                    },
                    "1": {
                      "text": "WorkInProgress",
                      "color": "green",
                      "index": 1
                    },
                    "2": {
                      "text": "Done",
                      "color": "dark-green",
                      "index": 2
                    },
                    "3": {
                      "text": "Discarded",
                      "color": "orange",
                      "index": 3
                    },
                    "4": {
                      "text": "Fault",
                      "color": "red",
                      "index": 4
                    }
                  }
                }
              ],
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "stepAfter",
                "lineWidth": 2,
                "fillOpacity": 15,
                "showPoints": "never",
                "spanNulls": true,
                "axisSoftMin": 0,
                "axisSoftMax": 4
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "mergeValues": true,
            "showValue": "auto",
            "alignValue": "center",
            "rowHeight": 0.9,
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstation = \"${station}\"\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_Status_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  // PERF: aggregateWindow must directly follow the filters so the planner can\n  // fold it into ReadWindowAggregate. The previous keep/map/group in between\n  // blocked that and forced every raw point through the Flux interpreter.\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  // Re-merge the per-writer results into the single series this panel showed\n  // before. last() is associative, so the rendered values are unchanged.\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |\u003e map(fn: (r) =\u003e ({_time: r._time, _value: float(v: r._value)}))\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"status\")"
            }
          ]
        },
        {
          "id": 50,
          "title": "Actual cycle time - $station",
          "type": "timeseries",
          "description": "Dashed line marks the 6 s ideal cycle time for the Munich line.",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 16
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "pointSize": 5,
                "spanNulls": true,
                "gradientMode": "none",
                "axisPlacement": "auto",
                "axisSoftMin": 0,
                "thresholdsStyle": {
                  "mode": "dashed"
                }
              },
              "unit": "ms",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 6000
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "multi",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstation = \"${station}\"\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_ActualCycleTime_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  // PERF: aggregateWindow must directly follow the filters so the planner can\n  // fold it into ReadWindowAggregate. The previous keep/map/group in between\n  // blocked that and forced every raw point through the Flux interpreter.\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  // Re-merge the per-writer results into the single series this panel showed\n  // before. last() is associative, so the rendered values are unchanged.\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |\u003e map(fn: (r) =\u003e ({_time: r._time, _value: float(v: r._value)}))\n  |\u003e set(key: \"_field\", value: \"actual\")"
            }
          ]
        },
        {
          "id": 20,
          "title": "Manufactured products",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 24
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "pointSize": 5,
                "spanNulls": true,
                "gradientMode": "none",
                "axisPlacement": "auto",
                "axisSoftMin": 0
              },
              "unit": "short"
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "multi",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstation = \"${station}\"\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_NumberOfManufacturedProducts_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  // PERF: aggregateWindow must directly follow the filters so the planner can\n  // fold it into ReadWindowAggregate. The previous keep/map/group in between\n  // blocked that and forced every raw point through the Flux interpreter.\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  // Re-merge the per-writer results into the single series this panel showed\n  // before. last() is associative, so the rendered values are unchanged.\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |\u003e map(fn: (r) =\u003e ({_time: r._time, _value: float(v: r._value)}))\n  |\u003e set(key: \"_field\", value: \"manufactured\")"
            }
          ]
        },
        {
          "id": 21,
          "title": "Discarded products",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 32
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "pointSize": 5,
                "spanNulls": true,
                "gradientMode": "none",
                "axisPlacement": "auto",
                "axisSoftMin": 0
              },
              "unit": "short"
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "multi",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstation = \"${station}\"\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_NumberOfDiscardedProducts_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  // PERF: aggregateWindow must directly follow the filters so the planner can\n  // fold it into ReadWindowAggregate. The previous keep/map/group in between\n  // blocked that and forced every raw point through the Flux interpreter.\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  // Re-merge the per-writer results into the single series this panel showed\n  // before. last() is associative, so the rendered values are unchanged.\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |\u003e map(fn: (r) =\u003e ({_time: r._time, _value: float(v: r._value)}))\n  |\u003e set(key: \"_field\", value: \"discarded\")"
            }
          ]
        },
        {
          "id": 30,
          "title": "Energy consumption",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 40
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "pointSize": 5,
                "spanNulls": true,
                "gradientMode": "none",
                "axisPlacement": "auto",
                "axisSoftMin": 0
              },
              "unit": "kwatth"
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "multi",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstation = \"${station}\"\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_EnergyConsumption_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  // PERF: aggregateWindow must directly follow the filters so the planner can\n  // fold it into ReadWindowAggregate. The previous keep/map/group in between\n  // blocked that and forced every raw point through the Flux interpreter.\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  // Re-merge the per-writer results into the single series this panel showed\n  // before. last() is associative, so the rendered values are unchanged.\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |\u003e map(fn: (r) =\u003e ({_time: r._time, _value: float(v: r._value)}))\n  |\u003e set(key: \"_field\", value: \"kWh\")"
            }
          ]
        },
        {
          "id": 31,
          "title": "Pressure",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 48
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "pointSize": 5,
                "spanNulls": true,
                "gradientMode": "none",
                "axisPlacement": "auto",
                "axisSoftMin": 0
              },
              "unit": "pressurembar"
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "multi",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstation = \"${station}\"\n\n// DataSetWriterIds belonging to this station, taken from the metadata stream.\n// Metadata is republished every MetadataSendInterval (300s), the 2d lookback is\n// deliberately generous: it is effectively how long the publisher may stop\n// publishing metadata before every panel goes blank. Do not shrink it without\n// considering that trade-off.\n// PERF: metaName is a tag, so a regex predicate is pushed into the storage\n// engine; strings.containsStr() was evaluated row-by-row over every metadata\n// point in the lookback window.\nstationRx = regexp.compile(v: station)\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ stationRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\n// PERF: a regex predicate on a tag can be pushed into the storage engine,\n// whereas contains() is evaluated row-by-row in Flux and blocks all pushdown.\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_Pressure_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  // PERF: aggregateWindow must directly follow the filters so the planner can\n  // fold it into ReadWindowAggregate. The previous keep/map/group in between\n  // blocked that and forced every raw point through the Flux interpreter.\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  // Re-merge the per-writer results into the single series this panel showed\n  // before. last() is associative, so the rendered values are unchanged.\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |\u003e map(fn: (r) =\u003e ({_time: r._time, _value: float(v: r._value)}))\n  |\u003e set(key: \"_field\", value: \"pressure\")"
            }
          ]
        }
      ]
    }
  # ---------------------------------------------------------------------------
  # Modbus simulator dashboard.
  #
  # This is a SEPARATE dashboard rather than another entry in the "station"
  # dropdown of production-line-oee.json on purpose: the modbus simulator is a
  # non-OPC UA device surfaced through UA Edge Translator and publishes a
  # completely different set of nodes (see the 'modbus-thing-description'
  # ConfigMap in edge.yaml). It has no OEE, Status or product-count nodes, so
  # adding it to that dropdown would leave ~10 of the 13 panels there blank.
  #
  # The writer-lookup preamble in every query is identical to the OEE dashboard
  # (metadata lookback -2d, regex pushdown on the metaName/datasetWriterId tags);
  # see the PERF comments there for why it is shaped this way.
  # ---------------------------------------------------------------------------
  modbus-simulator.json: |
    {
      "annotations": {
        "list": []
      },
      "editable": true,
      "graphTooltip": 1,
      "schemaVersion": 39,
      "title": "Modbus Simulator",
      "uid": "modbus-simulator",
      "tags": [
        "opcua",
        "modbus",
        "edge-translator"
      ],
      "time": {
        "from": "now-6h",
        "to": "now"
      },
      "refresh": "30s",
      "templating": {
        "list": []
      },
      "panels": [
        {
          "id": 100,
          "title": "Temperature",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true,
                "axisPlacement": "auto"
              },
              "unit": "celsius",
              "decimals": 1
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_Temperature_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: float(v: r._value)}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"temperature\")"
            }
          ]
        },
        {
          "id": 101,
          "title": "Pressure",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 8
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true,
                "axisPlacement": "auto"
              },
              "unit": "pressurebar",
              "decimals": 2,
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "red",
                    "value": 4.9
                  }
                ]
              }
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_Pressure_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: float(v: r._value)}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"pressure\")"
            }
          ]
        },
        {
          "id": 102,
          "title": "Flow rate",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 16
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true,
                "axisPlacement": "auto"
              },
              "unit": "flowlpm",
              "decimals": 1
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_FlowRate_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: float(v: r._value)}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"flowrate\")"
            }
          ]
        },
        {
          "id": 103,
          "title": "Energy consumption",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 24
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true,
                "axisPlacement": "auto"
              },
              "unit": "kwatth",
              "decimals": 2
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\n// EnergyConsumption is a monotonically increasing counter, so last() (not mean)\n// is the meaningful aggregate within a window.\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_EnergyConsumption_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: float(v: r._value)}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"energy\")"
            }
          ]
        },
        {
          "id": 104,
          "title": "Motor speed",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 32
          },
          "fieldConfig": {
            "defaults": {
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "linear",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true,
                "axisPlacement": "auto"
              },
              "unit": "rotrpm",
              "decimals": 0
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_MotorSpeed_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: float(v: r._value)}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"motorspeed\")"
            }
          ]
        },
        {
          "id": 105,
          "title": "Machine state",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 40
          },
          "fieldConfig": {
            "defaults": {
              "mappings": [
                {
                  "type": "value",
                  "options": {
                    "0": {
                      "text": "Stopped",
                      "color": "blue",
                      "index": 0
                    },
                    "1": {
                      "text": "Running",
                      "color": "green",
                      "index": 1
                    },
                    "2": {
                      "text": "Fault",
                      "color": "red",
                      "index": 2
                    }
                  }
                }
              ],
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "stepAfter",
                "lineWidth": 2,
                "fillOpacity": 15,
                "showPoints": "never",
                "spanNulls": true,
                "axisSoftMin": 0,
                "axisSoftMax": 2
              },
              "decimals": 0
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\n// Discrete enum: last() within the window, never mean().\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_MachineState_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: float(v: r._value)}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"machinestate\")"
            }
          ]
        },
        {
          "id": 106,
          "title": "Running",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 48
          },
          "fieldConfig": {
            "defaults": {
              "mappings": [
                {
                  "type": "value",
                  "options": {
                    "0": {
                      "text": "Stopped",
                      "color": "red",
                      "index": 0
                    },
                    "1": {
                      "text": "Running",
                      "color": "green",
                      "index": 1
                    }
                  }
                }
              ],
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "stepAfter",
                "lineWidth": 2,
                "fillOpacity": 15,
                "showPoints": "never",
                "spanNulls": true,
                "axisSoftMin": 0,
                "axisSoftMax": 1
              },
              "decimals": 0
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\n// Boolean node: aggregate with last(), then project to 0/1 for plotting.\n// float() cannot convert a bool, hence the conditional.\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_Running_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: if r._value then 1.0 else 0.0}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"running\")"
            }
          ]
        },
        {
          "id": 107,
          "title": "Fault active",
          "type": "timeseries",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 8,
            "w": 24,
            "x": 0,
            "y": 56
          },
          "fieldConfig": {
            "defaults": {
              "mappings": [
                {
                  "type": "value",
                  "options": {
                    "0": {
                      "text": "OK",
                      "color": "green",
                      "index": 0
                    },
                    "1": {
                      "text": "Fault",
                      "color": "red",
                      "index": 1
                    }
                  }
                }
              ],
              "custom": {
                "drawStyle": "line",
                "lineInterpolation": "stepAfter",
                "lineWidth": 2,
                "fillOpacity": 15,
                "showPoints": "never",
                "spanNulls": true,
                "axisSoftMin": 0,
                "axisSoftMax": 1
              },
              "decimals": 0
            },
            "overrides": []
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "refId": "A",
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\nstationRx = regexp.compile(v: \"modbus-simulator\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |> range(start: -2d)\n  |> filter(fn: (r) => r._measurement == \"opcua_metadata\")\n  |> filter(fn: (r) => r.metaName =~ stationRx)\n  |> keep(columns: [\"datasetWriterId\"])\n  |> group()\n  |> distinct(column: \"datasetWriterId\")\n  |> findColumn(fn: (key) => true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\n// The simulator trips this coil whenever Pressure crosses 4.9 bar.\nfrom(bucket: \"mqtt\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"opcua_pubsub\" and r._field == \"Payload_FaultActive_Value\")\n  |> filter(fn: (r) => r.datasetWriterId =~ writersRx)\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> group()\n  |> aggregateWindow(every: v.windowPeriod, fn: last, createEmpty: false)\n  |> map(fn: (r) => ({_time: r._time, _value: if r._value then 1.0 else 0.0}))\n  |> keep(columns: [\"_time\", \"_value\"])\n  |> yield(name: \"faultactive\")"
            }
          ]
        }
      ]
    }
  # ---------------------------------------------------------------------------
  # UA Cloud Publisher diagnostics dashboard.
  #
  # The Publisher publishes its own health as OPC UA nodes under
  # nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), which flow
  # through the same data/# topic as the production line telemetry. The writer
  # lookup in every panel therefore matches "CloudPublisher" in metaName rather
  # than a station name.
  #
  # These are the counters to check first when telemetry stops arriving:
  # queue depth and enqueue failures show back-pressure, send failures and
  # stored-messages show broker trouble.
  # ---------------------------------------------------------------------------
  publisher-diagnostics.json: |
    {
      "annotations": {
        "list": [
        ]
      },
      "editable": true,
      "graphTooltip": 1,
      "links": [
      ],
      "panels": [
        {
          "id": 1,
          "type": "stat",
          "title": "Broker connection",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 0,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "mappings": [
                {
                  "type": "value",
                  "options": {
                    "0": {
                      "text": "Disconnected",
                      "color": "red",
                      "index": 0
                    },
                    "1": {
                      "text": "Connected",
                      "color": "green",
                      "index": 1
                    }
                  }
                }
              ],
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "red",
                    "value": null
                  },
                  {
                    "color": "green",
                    "value": 1
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "background",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_ConnectedToBroker_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: if r._value then 1.0 else 0.0}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"connectedtobroker\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 2,
          "type": "stat",
          "title": "OPC UA sessions",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 6,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_NumOpcSessions_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"numopcsessions\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 3,
          "type": "stat",
          "title": "OPC UA subscriptions",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 12,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_NumOpcSubscriptions_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"numopcsubscriptions\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 4,
          "type": "stat",
          "title": "OPC UA monitored items",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 18,
            "y": 0
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_NumOpcMonitoredItems_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"numopcmonitoreditems\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 5,
          "type": "stat",
          "title": "Internal queue depth",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 0,
            "y": 4
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_QueueCount_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"queuecount\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 6,
          "type": "stat",
          "title": "Enqueue failures",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 6,
            "y": 4
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "red",
                    "value": 1
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_EnqueueFailures_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"enqueuefailures\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 7,
          "type": "stat",
          "title": "Send failures",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 12,
            "y": 4
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "red",
                    "value": 1
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_BrokerMessageSendFailures_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"brokermessagesendfailures\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 8,
          "type": "stat",
          "title": "Stored messages left to send",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 18,
            "y": 4
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "red",
                    "value": 1
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_StoredMessagesLeftToSend_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"storedmessageslefttosend\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 9,
          "type": "stat",
          "title": "Messages sent",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 0,
            "y": 8
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_SentMessages_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"sentmessages\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 10,
          "type": "stat",
          "title": "Avg broker message size",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 6,
            "y": 8
          },
          "fieldConfig": {
            "defaults": {
              "unit": "decbytes",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_BrokerMessageSize_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"brokermessagesize\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 11,
          "type": "stat",
          "title": "Working set",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 4,
            "w": 6,
            "x": 12,
            "y": 8
          },
          "fieldConfig": {
            "defaults": {
              "unit": "decmbytes",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              },
              "decimals": 0
            },
            "overrides": [
            ]
          },
          "options": {
            "colorMode": "value",
            "graphMode": "none",
            "justifyMode": "auto",
            "orientation": "auto",
            "textMode": "auto",
            "reduceOptions": {
              "calcs": [
                "lastNotNull"
              ],
              "fields": "",
              "values": false
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_CurrentWorkingSetMBs_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e sort(columns: [\"_time\"])\n  |\u003e last()\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"currentworkingsetmbs\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 12,
          "type": "timeseries",
          "title": "Broker messages / second",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 7,
            "w": 8,
            "x": 0,
            "y": 15
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "custom": {
                "drawStyle": "line",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true
              },
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_BrokerMessagesSecond_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"brokermessagessecond\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 13,
          "type": "timeseries",
          "title": "Notifications / second",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 7,
            "w": 8,
            "x": 8,
            "y": 15
          },
          "fieldConfig": {
            "defaults": {
              "unit": "none",
              "custom": {
                "drawStyle": "line",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true
              },
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_NumOpcMonitoredItemsSecond_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"numopcmonitoreditemssecond\")",
              "refId": "A"
            }
          ]
        },
        {
          "id": 14,
          "type": "timeseries",
          "title": "Average message latency",
          "datasource": {
            "type": "influxdb",
            "uid": "influxdb"
          },
          "gridPos": {
            "h": 7,
            "w": 8,
            "x": 16,
            "y": 15
          },
          "fieldConfig": {
            "defaults": {
              "unit": "ms",
              "custom": {
                "drawStyle": "line",
                "lineWidth": 2,
                "fillOpacity": 10,
                "showPoints": "never",
                "spanNulls": true
              },
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  {
                    "color": "text",
                    "value": null
                  }
                ]
              }
            },
            "overrides": [
            ]
          },
          "options": {
            "legend": {
              "displayMode": "list",
              "placement": "bottom",
              "showLegend": false
            },
            "tooltip": {
              "mode": "single",
              "sort": "none"
            }
          },
          "targets": [
            {
              "datasource": {
                "type": "influxdb",
                "uid": "influxdb"
              },
              "query": "import \"strings\"\nimport \"regexp\"\n\n// The Publisher publishes its own diagnostics as OPC UA nodes under\n// nsu=http://opcfoundation.org/UA/CloudPublisher/ (Diagnostics.cs), so the\n// writer lookup keys on that namespace rather than a production line station.\npublisherRx = regexp.compile(v: \"CloudPublisher\")\n\nwriters =\n  from(bucket: \"mqtt\")\n  |\u003e range(start: -2d)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_metadata\")\n  |\u003e filter(fn: (r) =\u003e r.metaName =~ publisherRx)\n  |\u003e keep(columns: [\"datasetWriterId\"])\n  |\u003e group()\n  |\u003e distinct(column: \"datasetWriterId\")\n  |\u003e findColumn(fn: (key) =\u003e true, column: \"_value\")\n\nwritersRx = regexp.compile(v: \"^(\" + strings.joinStr(arr: writers, v: \"|\") + \")$\")\n\nfrom(bucket: \"mqtt\")\n  |\u003e range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |\u003e filter(fn: (r) =\u003e r._measurement == \"opcua_pubsub\" and r._field == \"Payload_BrokerMessageLatency_Value\")\n  |\u003e filter(fn: (r) =\u003e r.datasetWriterId =~ writersRx)\n  |\u003e map(fn: (r) =\u003e ({r with _value: float(v: r._value)}))\n  |\u003e group()\n  |\u003e aggregateWindow(every: v.windowPeriod, fn: mean, createEmpty: false)\n  |\u003e keep(columns: [\"_time\", \"_value\"])\n  |\u003e yield(name: \"brokermessagelatency\")",
              "refId": "A"
            }
          ]
        }
      ],
      "refresh": "30s",
      "schemaVersion": 41,
      "tags": [
        "opcua",
        "publisher",
        "diagnostics"
      ],
      "templating": {
        "list": [
        ]
      },
      "time": {
        "from": "now-1h",
        "to": "now"
      },
      "timepicker": {
      },
      "timezone": "",
      "title": "UA Cloud Publisher Diagnostics",
      "uid": "publisher-diagnostics",
      "version": 2,
      "weekStart": ""
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: cloud
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      securityContext:
        fsGroup: 472
      initContainers:
        # Ensure the hostPath data dir is writable by the grafana user (uid/gid 472)
        - name: init-chown-data
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "chown -R 472:472 /var/lib/grafana"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: data
              mountPath: /var/lib/grafana
      containers:
        - name: grafana
          image: grafana/grafana:13.1.1
          imagePullPolicy: IfNotPresent
          env:
            - name: GF_SECURITY_ADMIN_USER
              value: "${IOT_USERNAME}"
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: "${IOT_PASSWORD}"
            # Land on the OEE dashboard instead of the empty Grafana home page.
            # The path is the one the file provisioner serves it from; the uid in
            # the dashboard JSON is "production-line-oee".
            - name: GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH
              value: "/var/lib/grafana/dashboards/production-line-oee.json"
            # Consumed by the provisioned datasource (token interpolation)
            - name: INFLUX_TOKEN
              valueFrom:
                secretKeyRef:
                  name: influxdb-auth
                  key: INFLUX_TOKEN
          ports:
            - name: http
              containerPort: 3000
          volumeMounts:
            - name: data
              mountPath: /var/lib/grafana
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            - name: dashboard-provider
              mountPath: /etc/grafana/provisioning/dashboards
            - name: dashboards
              mountPath: /var/lib/grafana/dashboards
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: data
          hostPath:
            path: "/grafana"
        - name: datasources
          configMap:
            name: grafana-datasources
        - name: dashboard-provider
          configMap:
            name: grafana-dashboard-provider
        - name: dashboards
          configMap:
            name: grafana-dashboards
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ua-cloudaction
  namespace: cloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ua-cloudaction
  template:
    metadata:
      labels:
        app: ua-cloudaction
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: ua-cloudaction
        image: ghcr.io/opcfoundation/ua-cloudaction:main
        imagePullPolicy: IfNotPresent
        env:
        # --- Web UI / OPC UA Web API login ---
        - name: ADMIN_USERNAME
          value: "${IOT_USERNAME}"
        - name: ADMIN_PASSWORD
          value: "${IOT_PASSWORD}"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        # --- OPC UA Web API rate limiter (per client IP, fixed window) ---
        - name: RATE_LIMIT_PERMIT
          value: "60"
        - name: RATE_LIMIT_WINDOW_SECONDS
          value: "60"
        # --- Data source: query InfluxDB instead of Azure Data Explorer ---
        - name: DATA_SOURCE
          value: "InfluxDB"
        - name: INFLUX_URL
          value: "http://influxdb.cloud.svc.cluster.local:8086"
        - name: INFLUX_ORG
          value: "iot"
        - name: INFLUX_BUCKET
          value: "mqtt"
        - name: INFLUX_MEASUREMENT
          value: "opcua_pubsub"
        # Metadata measurement written by the second Telegraf mqtt_consumer input.
        # Browse joins it to INFLUX_MEASUREMENT on the "datasetWriterId" tag to
        # resolve each tag's OPC UA namespace URI from its "metaName" value.
        - name: INFLUX_METADATA_MEASUREMENT
          value: "opcua_metadata"
        # Field to evaluate and threshold that triggers the OPC UA method call.
        # Adjust INFLUX_FIELD to match a numeric field your assets publish.
        - name: INFLUX_FIELD
          value: "Payload_Pressure_Value"
        - name: INFLUX_THRESHOLD
          value: "4000"
        - name: INFLUX_RANGE
          value: "-1m"
        - name: INFLUX_TOKEN
          valueFrom:
            secretKeyRef:
              name: influxdb-auth
              key: INFLUX_TOKEN
        # --- Transport: reach UA Cloud Commander over the Mosquitto MQTT broker ---
        - name: MESSAGING_PLATFORM
          value: "MQTT"
        # Send the full OPC UA PubSub ActionRequest envelope to UA Cloud Commander (not the AIO RPC body)
        - name: MQTT_TARGET
          value: "UACloudCommander"
        - name: BROKER_NAME
          value: "mosquitto.cloud.svc.cluster.local"
        - name: MQTT_PORT
          value: "8883"
        - name: MQTT_USE_TLS
          value: "true"
        # Mosquitto uses a self-signed certificate; skip verification (matches Telegraf's insecure_skip_verify)
        - name: MQTT_TLS_INSECURE
          value: "true"
        - name: BROKER_USERNAME
          value: "${IOT_USERNAME}"
        - name: BROKER_PASSWORD
          value: "${IOT_PASSWORD}"
        # Topics UA Cloud Commander listens/responds on (it subscribes to commands, replies on responses)
        - name: TOPIC
          value: "commands"
        - name: RESPONSE_TOPIC
          value: "responses"
        - name: UA_CLOUD_COMMANDER_ID
          value: "UACloudCommander"
        - name: REQUESTOR_ID
          value: "UACloudAction"
        # --- OPC UA method to invoke on high value (target server reachable by UA Cloud Commander) ---
        # This is the assembly station's pressure-relief method
        - name: UA_SERVER_ENDPOINT
          value: "opc.tcp://assembly.munich/"
        - name: UA_SERVER_METHOD_ID
          value: "ns=2;i=435"
        - name: UA_SERVER_OBJECT_ID
          value: "ns=2;i=424"
        - name: UA_SERVER_APPLICATION_NAME
          value: "assembly"
        - name: UA_SERVER_LOCATION_NAME
          value: "munich"
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: ua-cloudaction
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: ua-cloudaction
  ports:
    - name: http
      port: 8082
      targetPort: 8080
---
# ---------------------------------------------------------------------------
# PostgreSQL - backing store for the UA Cloud Library
#
# The Cloud Library keeps BOTH its relational data and the uploaded nodeset
# files in PostgreSQL (there is no separate blob store), so this database is
# the single source of truth for everything you upload. It is therefore backed
# by a hostPath volume like InfluxDB, and uses the same Recreate strategy: a
# single-writer database on a hostPath cannot tolerate two pods holding the
# same data directory during a rolling update.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: cloudlib-postgres-auth
  namespace: cloud
type: Opaque
stringData:
  POSTGRES_DB: "uacloudlib"
  POSTGRES_USER: "${IOT_USERNAME}"
  POSTGRES_PASSWORD: "${IOT_PASSWORD}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudlib-postgres
  namespace: cloud
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: cloudlib-postgres
  template:
    metadata:
      labels:
        app: cloudlib-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17.6-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: postgres
          envFrom:
            - secretRef:
                name: cloudlib-postgres-auth
          env:
            # The official image refuses to initialise into a non-empty directory
            # such as a hostPath root that already contains lost+found, so keep the
            # cluster in a dedicated subdirectory.
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\""]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\""]
            initialDelaySeconds: 30
            periodSeconds: 20
      volumes:
        - name: "data"
          hostPath:
            path: "/cloudlib-postgres"
---
apiVersion: v1
kind: Service
metadata:
  name: cloudlib-postgres
  namespace: cloud
spec:
  # ClusterIP: the database is only ever reached from inside the cluster.
  # Do NOT change this to LoadBalancer - it would expose PostgreSQL on the
  # node IP with a credential that is shared with the rest of the demo.
  type: ClusterIP
  selector:
    app: cloudlib-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
# ---------------------------------------------------------------------------
# UA Cloud Library - a self-hosted instance of the OPC Foundation's store of
# OPC UA Information Models (https://uacloudlibrary.opcfoundation.org).
#
# PURPOSE HERE: this instance is the store for EU DIGITAL PRODUCT PASSPORTS.
# UA Data Processor computes a Product Carbon Footprint and a Battery Passport
# from live production telemetry and publishes them here as OPC UA Information
# Models, one per product; the Cloud Library provides the storage, versioning,
# search and REST retrieval that a passport registry needs.
#
# Running it locally additionally keeps passports and proprietary models on your
# own infrastructure, and removes the dependency on the public internet.
#
# EMAIL VERIFICATION IS DISABLED. The server enables it only when an email
# sender API key is present:
#     options.SignIn.RequireConfirmedAccount =
#         !string.IsNullOrEmpty(Configuration["EmailSenderAPIKey"])
# so deliberately NOT setting EmailSenderAPIKey below lets newly registered
# users sign in immediately, with no outbound mail server. The consequence is
# that self-registration is open to anyone who can reach the UI, so keep this
# instance on a trusted network. See the security section of the README.
#
# ARM64 NOTE: the image is multi-arch (linux/amd64 + linux/arm64) as of 1.1.34,
# so it runs on the Raspberry Pi. Tags 1.1.33 and earlier are amd64-only and
# fail with "exec format error" on arm64.
# ---------------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ua-cloudlibrary
  namespace: cloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ua-cloudlibrary
  template:
    metadata:
      labels:
        app: ua-cloudlibrary
    spec:
      initContainers:
        # Entity Framework applies its migrations at startup and simply throws if
        # the database is not accepting connections yet, so wait for PostgreSQL
        # rather than relying on CrashLoopBackOff to retry.
        - name: wait-for-postgres
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          command:
            - "sh"
            - "-c"
            - "until nc -z cloudlib-postgres 5432; do echo waiting for postgres; sleep 5; done"
      containers:
        - name: ua-cloudlibrary
          image: ghcr.io/opcfoundation/ua-cloudlibrary:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ASPNETCORE_HTTP_PORTS
              value: "8080"
            # Npgsql connection string. The key name must match exactly, as the
            # server reads ConnectionStrings:CloudLibraryPostgreSQL and throws a
            # descriptive InvalidOperationException when it is missing.
            - name: ConnectionStrings__CloudLibraryPostgreSQL
              value: "Host=cloudlib-postgres;Port=5432;Database=uacloudlib;Username=${IOT_USERNAME};Password=${IOT_PASSWORD}"
            # Credentials for the Cloud Library's own REST/GraphQL API. These are
            # what UA Data Processor authenticates with below.
            - name: ServiceUsername
              value: "${IOT_USERNAME}"
            - name: ServicePassword
              value: "${IOT_PASSWORD}"
            # EmailSenderAPIKey is intentionally NOT set - see the note above.
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: ua-cloudlibrary
  namespace: cloud
spec:
  type: LoadBalancer
  selector:
    app: ua-cloudlibrary
  ports:
    - name: http
      port: 8083
      targetPort: 8080
---
# ---------------------------------------------------------------------------
# UA Data Processor - calculates a Product Carbon Footprint (PCF) and a Digital
# Battery Passport from the OPC UA telemetry produced by the simulated
# production line, and publishes the results as OPC UA Information Models into
# the UA Cloud Library above, which acts as the Digital Product Passport store.
#
# The processor picks its historian automatically: when INFLUX_TOKEN is set it
# reads from InfluxDB, otherwise it falls back to Azure Data Explorer. Setting
# the token below is therefore what configures it for InfluxDB.
#
# It reads the same two measurements the Grafana dashboards use, joining
# telemetry to metadata on the shared datasetWriterId tag:
#   opcua_pubsub   - the OPC UA PubSub telemetry
#   opcua_metadata - the matching metadata, carrying the DataSetName in metaName
#
# NOTE: when running against InfluxDB the processor only evaluates the MUNICH
# production line, because that is the only line this reference solution
# simulates. The Seattle line exists in the Azure Data Explorer variant only.
# ---------------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ua-dataprocessor
  namespace: cloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ua-dataprocessor
  template:
    metadata:
      labels:
        app: ua-dataprocessor
    spec:
      initContainers:
        - name: wait-for-influxdb
          image: busybox:1.38
          imagePullPolicy: IfNotPresent
          command:
            - "sh"
            - "-c"
            - "until nc -z influxdb 8086; do echo waiting for influxdb; sleep 5; done"
      containers:
        - name: ua-dataprocessor
          image: ghcr.io/opcfoundation/ua-dataprocessor:main
          imagePullPolicy: IfNotPresent
          env:
            # --- InfluxDB (setting INFLUX_TOKEN selects InfluxDB over ADX) ---
            - name: INFLUX_URL
              value: "http://influxdb.cloud.svc.cluster.local:8086"
            - name: INFLUX_TOKEN
              valueFrom:
                secretKeyRef:
                  name: influxdb-auth
                  key: INFLUX_TOKEN
            - name: INFLUX_ORG
              value: "iot"
            - name: INFLUX_BUCKET
              value: "mqtt"
            - name: INFLUX_MEASUREMENT
              value: "opcua_pubsub"
            - name: INFLUX_METADATA_MEASUREMENT
              value: "opcua_metadata"
            # Queries that scan several days of data comfortably exceed the
            # client's 10s default, which surfaces as a TaskCanceledException.
            - name: INFLUX_TIMEOUT_SECONDS
              value: "120"
            # --- UA Cloud Library (where the resulting models are published) ---
            - name: UA_CLOUD_LIBRARY_URL
              value: "http://ua-cloudlibrary.cloud.svc.cluster.local:8083"
            - name: UA_CLOUD_LIBRARY_USERNAME
              value: "${IOT_USERNAME}"
            - name: UA_CLOUD_LIBRARY_PASSWORD
              value: "${IOT_PASSWORD}"
            # --- WattTime (optional) ---
            # Without credentials the carbon-intensity lookup simply returns no
            # data and the PCF calculation is skipped; the pod stays healthy.
            # Sign up at https://watttime.org and add your credentials here to
            # enable real grid carbon intensity for the production line location.
            # - name: WATTTIME_USER
            #   value: "<your WattTime user>"
            # - name: WATTTIME_PASSWORD
            #   value: "<your WattTime password>"
